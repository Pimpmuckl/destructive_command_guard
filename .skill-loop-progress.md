# OMP Alien-Artifact Fifty-Pass Ledger

This file is the authoritative progress record for the user-requested fifty full
iterations over dcg's Oh My Pi (`omp`) integration. It intentionally remains in
the repository after the loop so the audit is reproducible.

## Campaign

- Skills: `alien-graveyard` candidate harvest, then `alien-artifact-coding`
  proof/optimization passes, orchestrated with `repeatedly-apply-skill`
- Target: native OMP protocol bridge, install/uninstall lifecycle, path/profile
  semantics, diagnostics, documentation, and release-blocking conformance
- Total required passes: **50**
- Baseline commit: `b361301aa818f1fc97ebb34d1445350e08285de2`
- Started: `2026-08-24T13:04:58Z`
- Current pass: 38 of 50
- Governing constraint: no file deletion and no destructive Git/filesystem
  commands; every numbered pass is delegated serially, reviewed by the primary
  agent, verified proportionally, recorded here, and committed independently

## Baseline and decision model

- Source state: `main`, clean, equal to `origin/main` at campaign start.
- Host state: macOS load average `110.35 81.13 61.93`; local timing and heavy
  compilation are contaminated, so build/test work uses RCH and performance
  claims require a remote-host fingerprint.
- Binary state: the local `target/release/dcg` is a foreign Linux artifact and
  cannot serve as a macOS runtime baseline; a current remote release build was
  queued before optimization.
- Safety states: `{safe, destructive, indeterminate, infrastructure_fault}`.
- Bridge actions: `{allow, block, fail_open_with_diagnostic}`.
- Asymmetric losses (relative units): destructive false allow `1000`;
  indeterminate false allow `500`; safe false block `5`; safe infrastructure
  fail-open `1`; silent infrastructure failure `25`. Existing public policy is
  preserved: dcg decisions `deny`/`ask`/`indeterminate` block, while an absent or
  unrunnable dcg binary fails open visibly.
- Proof ladder: Tier A golden/contract invariants for every pass; Tier B
  property/metamorphic tests for parsers and path state machines; bounded model
  exploration for lifecycle/protocol state machines where it has positive EV.
- Optimization acceptance: one lever per pass, measured hotspot or concrete
  correctness risk, semantic-isomorphism argument, explicit fallback, and no
  material p99 regression. Clever mechanisms that cannot clear those gates are
  rejected rather than shipped.

## Graveyard candidate cards

| Candidate | Entry | Opportunity | Risk / fallback | Disposition |
|---|---|---:|---|---|
| Protocol transition automata | Session types, model checking (`§5.2`, `§6.18`) | 12.5 | State explosion; model only the four-state bridge | Adopt as tests/spec |
| Generated-source translation validation | Verified compilation (`§6.14`) | 10.0 | Brittle text assertions; execute the emitted bridge and retain golden invariants | Adopt |
| Property tests with shrinking | Property testing (`§6.12`) | 12.5 | Random-only false confidence; deterministic seeds plus boundary examples | Adopt selectively |
| Targeted lifecycle faults | LDFI (`§13.13`) | 10.0 | Incomplete lineage; enumerate explicit install/write/read/remove edges | Adopt |
| Minimal failure traces | Hierarchical delta debugging (`§6.15`) | 6.7 | Harness complexity; use only for observed failures | Conditional |
| Deterministic evidence ledger | Hindsight logging (`§3.10`) | 6.3 | Runtime/logging tax; keep evidence in tests and this ledger | Adopt offline |
| Capability-minimal child process | OCap (`§3.4`) | 8.3 | Environment stripping can break supported overrides; preserve documented `DCG_*` surface | Audit/measure |
| Proof-carrying generated artifact | PCC (`§11.9`) | 5.0 | Full formal proof is disproportionate; ownership marker plus executable contract tests | Lightweight form |
| COW/zygote child evaluator | Zygote (`§15.7`) | 1.7 | Whole-stack/Bun portability barrier; direct spawn remains safe fallback | Reject unless profiling overturns |
| Persistent evaluator daemon | Session/transport reuse | 1.9 | New trust boundary, stale policy/config, lifecycle complexity | Reject for this scope |

## Fifty serial missions

| Pass | Mission and proof obligation | Status | Commit / evidence |
|---:|---|---|---|
| 1 | Correct project-extension anchoring to OMP's cwd-only discovery; prove nested Git cwd does not silently install an unloaded file. | complete | `72e6c42`; 7 Rust + 10 targeted Bats green |
| 2 | Make Unix OMP configuration conflicts non-fatal to the overall installer while preserving truthful conflict status; add a full-flow mutation-sensitive test. | complete | `8f22b4b`; 15 focused Bats green |
| 3 | Make robot `--agent omp` apply `[agents.omp]` pack and allowlist policy, not attribution alone; prove positive and negative policy cases. | complete | `db6c649`; 2 robot-boundary tests + targeted clippy green |
| 4 | Propagate each OMP bash call's effective `cwd` into dcg so project config/allowlists are evaluated where the command executes. | complete | `8b94b9a`; generated-source + real child-cwd tests green |
| 5 | Distinguish default profiles from invalid/non-Unicode profiles and fail without writing where OMP itself rejects startup. | complete | `5d3431a`; 5 CLI profile tests + exact unit + targeted clippy green |
| 6 | Resolve the OMP shell-dialect contract on Windows and close any Cmd/PowerShell evasion without overmatching POSIX-only execution. | complete | `f0354a3`, `ae09248`; mutation red + final-byte exact remote green |
| 7 | Model the OMP bridge as a transition table over dcg exit code × parsed decision; prove every cell has an intentional outcome. | complete | `06ad9a6`, `ae671fc`, `a1bcae0`; 35-cell source contract, fmt/diff green; remote gate pending |
| 8 | Verify stdout/stderr framing: only robot JSON drives decisions, diagnostics remain visible, and no channel swap can silently allow a block. | complete | `2b20a2c`, `522f447`, `ba55a22`; mutation red + final source contract/fmt/diff green |
| 9 | Audit OMP `tool_call` structural validation (tool name, input object, command type/emptiness) with adversarial negative fixtures. | complete | `fe2cb7b`; mutation red + structural source contract/fmt/diff green |
| 10 | Build an executable replay corpus/certificate for the generated TypeScript bridge, not merely Rust string-contains assertions. | complete | `f93365d`, `6e7912c`, `adaa21d`, `0120da8`; exact Bun replay + mutant red green |
| 11 | Make all OMP installer/uninstaller tests hermetic against ambient profile/config/agent-dir variables so they cannot touch a live extension. | complete | `dc6588e`, `95a90d0`, `33392ea`, `4ab6326`; mutation red + Unix/PowerShell bodies green |
| 12 | Implement OMP's stale profile-derived `PI_CODING_AGENT_DIR` suppression while retaining genuine custom overrides. | complete | `2c280aa`, `97d8563`, `26e8fed`; unit + real-binary E2E + Unix/PowerShell parity + pre-fix red green |
| 13 | Prove `PI_CONFIG_DIR` normalization matches Node path semantics, including leading backslash behavior on POSIX and Windows. | complete | `0dd6616`, `3bea88c`, `af8c3fb`, `03205e0`; cross-language oracle + two live mutation reds green |
| 14 | Make PowerShell profile validation case-sensitive and align empty/invalid precedence with Rust, Unix, and upstream OMP. | complete | `8987923`; 14-row precedence oracle + exact two-row mutation red green |
| 15 | Separate extension ownership from extension health so doctor cannot accept a truncated/stale/disabled marker-only file. | complete | `93ca67b`..`eee534c`; exact-byte state table + scoped repair + JSON framing green |
| 16 | Make PowerShell ownership-marker matching case-sensitive and preserve near-marker user files. | complete | `e0c2cc5`; old-predicate red + ordinal positive/negative matrix + parse green |
| 17 | Include OMP in Unix uninstaller pre-confirmation inventory so an OMP-only installation is removed and disclosed. | complete | `2b675d6`; real-main inactive-profile red/green + read-only inventory/cleanup parity |
| 18 | Remove duplicate project cleanup candidates and prevent cwd-resolution failure from synthesizing a root-level target. | complete | `3669a5a`, `39e5123`, `cb86d66`; candidate-cardinality/static mutation proof + shell gates |
| 19 | Enumerate hidden inactive Windows profiles during uninstall while preserving bounded marker-only deletion. | complete | `a6a3bcd`, `2f30596`; official API contract + native-Windows hidden/system fixture, runtime pending CI |
| 20 | Make all OMP read/enumerate/delete failures truthful, non-duplicated, and reflected in summary success markers on Unix and Windows. | complete | `edc9112`, `36e714c`, `dc674d3`; no-op-remover mutation red/green + exact summary inverse |
| 21 | Add a bounded parent-side deadline for the generated child evaluator and prove a stuck process cannot hang every OMP bash call. | complete | `bcbb59d`, `84413d3`, `173faa8`, `afdce76`; native Bun kill/reap + exact Rust gates green |
| 22 | Restore persistent decision history for OMP bridge evaluations with correct `agent_type=omp` attribution. | complete | `bca8b99`, `5aa6fd8`, `887950e`; real blocked-exit SQLite persistence + agent isolation green |
| 23 | Require a real executable OMP binary for Unix auto-detection and bind version probing to that exact capability. | complete | `719cec9`, `12dfcff`; stale-state/function/nonexec mutation matrix + shell gates green |
| 24 | Bind the generated bridge to the installer-validated dcg executable instead of ambient `DCG_BIN`. | complete | `1a0f8cd`, `ff89fa8`; authority mutation red/green + exact Rust gates green |
| 25 | Isolate stdout, stderr, and exit observation faults so one rejected capability cannot erase a surviving safety verdict. | complete | `99d8e48`, `fc8db67`; direct Bun fault matrix + exact Rust gates green |
| 26 | Bound OMP robot output and child stream retention while preserving Unicode, drainage, and monotone blocking. | complete | `0412e88`; collector/envelope mutants + direct Bun probes + exact Rust gates green |
| 27 | Measure cold/warm Bun bridge tails and identify whether caching assumptions are real in OMP's extension lifetime. | complete | no code change; 24k warm + 8k exact-cold invocations, no >=3% general gain |
| 28 | Minimize child-process ambient authority/environment if compatible with documented dcg configuration and diagnostics. | complete | `6cd000b`; ambient-format mutation red + causal real-binary pair + exact Rust gates green |
| 29 | Profile generated-bridge startup file I/O/syscalls and remove redundant work if it clears the 3% practical-gain gate. | complete | no code change; two 12k-child A/B runs + syscall/import/helper evidence, no coherent >=3% gain |
| 30 | Produce a reproducible latency certificate tied to host/toolchain/source hash and the shipped hook budget. | complete | `45b737e`; frozen binary + schema-v4 9x300 paired certificate, 38/38 harness, 374/374 full E2E |
| 31 | Stress command length, Unicode, invalid-surrogate boundaries, and control characters through the full OMP-to-dcg byte path. | complete | `799b7b3`; 10 real-binary byte probes + 9-case Bun differential + exact Rust gates |
| 32 | Fault-inject malformed/partial robot JSON and prove exit/decision precedence cannot fail open after a safety verdict. | complete | `29df7ce`, `bce0303`, `bbbd0c3`, `4116997`; 35 transition + 70 framing cells, exact Bun replay + 5 mutants, exact Rust gates green |
| 33 | Fault-inject all relevant child exit codes and signal-like termination outcomes with exact diagnostics and decisions. | complete | `db1695d`, `c40403f`; 42 transitions + 20 termination shapes + 84 framing cells + 48 callbacks, exact Bun replay + 9 mutants, exact Rust gates green |
| 34 | Exercise child spawn failure, crash, stalled pipe, and timeout/deadline interactions; document bounded vs unbounded seams. | complete | `1362793`; 226-case exact Bun certificate + 14 mutants, absolute deadline and inherited-pipe proof, exact Rust gates green |
| 35 | Audit TOCTOU/binary replacement between install and tool call; preserve explicit operator override while surfacing trust limits. | complete | `bf4b66b`; path-only authority contract + focused Rust test/check + immutable audit |
| 36 | Test symlink, path alias, macOS `/private`, and Windows reparse-like ownership/path cases without broadening deletion authority. | complete | `289f565`; no-follow ownership + isolated real regression + exact Rust gates |
| 37 | Differentially test Windows drive, UNC, rooted, device-name, and separator cases across Rust and PowerShell resolvers. | complete | `d41abb5`; Bun raw-byte mutation red + exact Rust/runtime matrix green |
| 38 | Stress POSIX glob/newline/control characters in profile/config values; ensure no word-splitting or pattern injection. | pending | |
| 39 | Model concurrent install/uninstall interleavings and verify atomicity/ownership invariants under races. | pending | |
| 40 | Run cross-platform metamorphic parity checks over equivalent path/profile states for Rust, Bash, and PowerShell. | pending | |
| 41 | Simplify the generated TypeScript bridge isomorphically, removing only complexity proven redundant by the transition model. | pending | |
| 42 | Audit duplicate path/profile logic for drift; centralize only when coupling cost is lower than cross-language contract tests. | pending | |
| 43 | Mechanically validate documentation examples and claims against generated source, CLI flags, paths, and live harness output. | pending | |
| 44 | Mutation-audit OMP tests for vacuity: each safety assertion must fail under a targeted one-line semantic mutation. | pending | |
| 45 | Add or strengthen bounded property/fuzz coverage for the profile/path state machine with reproducible counterexamples. | pending | |
| 46 | Add model-based lifecycle sequence tests for absent, foreign, owned, refreshed, partially failed, and removed states. | pending | |
| 47 | Produce a deterministic evidence ledger for installer decisions and verify summary claims are derivable from recorded state. | pending | |
| 48 | Review the OMP integration's trusted computing base and reduce ambient authority/unsafe assumptions with explicit residual risks. | pending | |
| 49 | Perform a final cross-seam adversarial rescan of Rust, Bash, PowerShell, generated TS, tests, docs, and CI wiring. | pending | |
| 50 | Synthesize proof, benchmark, and convergence evidence; rerun every release-blocking OMP gate and close only with no unresolved in-scope defect. | pending | |

## Pass record template

Each completed row links to a detailed record appended below with: fresh agent,
scope inspected, concrete findings, change or three-plus zero-change checks,
isomorphism/error/resource argument, verification, residual risk, and commit.

## Pass 1 record: cwd-only project discovery

- Fresh agent: `omp_pass_01_cwd_anchor`.
- Finding: dcg resolved project extensions through the Git root, while upstream
  OMP loads native project extensions only from the exact process cwd and does
  not require Git or walk ancestors.
- Change: Rust install/doctor resolution and Unix uninstallation now use the
  current cwd; CLI help, README, and agent documentation state the same
  contract.
- Mutation-sensitive evidence: nested-Git tests plant an ancestor `.git`; old
  code would write or accept the ancestor extension, while the new assertions
  require the nested cwd and reject ancestor-only doctor coverage. A non-Git
  install case proves removal of the old Git precondition. Unix tests prove an
  ancestor extension is preserved and a cwd extension is removed.
- Isomorphism/error/resource proof: user/profile resolution, marker ownership,
  atomic writes, and decision behavior are unchanged. `current_dir` failures
  propagate; successful path resolution is now a constant-size join instead of
  an ancestor search.
- Verification: remote `cli_e2e omp` slice on `vmi1293453` reported 7 passed,
  0 failed; targeted OMP uninstaller slice reported 10 passed; primary-agent
  rerun of the two new Bats cases reported 2 passed; `cargo fmt --check`,
  `bash -n uninstall.sh`, and `git diff --check` passed.
- Residual: PowerShell project cleanup parity is intentionally deferred to its
  own bounded pass.

## Pass 2 record: nonfatal, truthful Unix configuration conflict

- Fresh agent: `omp_pass_02_installer_conflict`.
- Finding: under `set -e`, the production installer called `configure_omp`
  bare. Its intended `conflict`/`failed` return values therefore terminated the
  installer before the code that renders those exact states.
- Change: the call boundary now explicitly consumes the nonzero status after
  `OMP_STATUS`/`OMP_FAILURE_REASON` have recorded it; the function's result and
  all summary branches remain intact.
- Mutation-sensitive evidence: the new full-installer fixture failed at its
  exit-status assertion with the production patch absent. It now proves the
  exact `install --omp --force` delegation occurred, the final success section
  was reached, the ownership conflict was printed, and the foreign extension
  remained byte-identical.
- Isomorphism/error/resource proof: success and skip transitions are unchanged;
  only `conflict-recorded -> summary-rendered` and `failed-recorded ->
  summary-rendered` replace an unintended shell abort. No new process,
  allocation, retry, or measurable hot-path cost was introduced.
- Verification: primary-agent rerun of all 15 OMP-filtered Bats cases passed;
  `bash -n install.sh`, `shellcheck install.sh`, and `git diff --check` passed.
- Residual: the nearby OpenCode call boundary has similar shape but is outside
  this OMP-only campaign/pass.

## Pass 3 record: agent-specific policy at the robot boundary

- Fresh agents: `omp_pass_03_agent_policy` established the mutation-sensitive
  failing fixture; after that agent was interrupted, the fresh replacement
  `omp_pass_03_policy_repair` completed the bounded implementation and inverse
  cases.
- Finding: `dcg --robot test --agent omp` used the explicit selector only for
  result attribution. Unlike hook mode, it ignored `[agents.omp]`
  `extra_packs`, `disabled_packs`, `additional_allowlist`, and
  `disabled_allowlist` policy.
- Change: agent pack exclusion and layered-allowlist application are canonical
  `Config` operations shared by hook and robot paths. Robot evaluation now
  resolves the selected agent's policy; invocation-local `--with-packs` is
  deliberately re-applied last and remains the caller's explicit override.
- Mutation-sensitive evidence: before the implementation, the real CLI/stdin
  fixture expected OMP's `containers.docker` extra pack to block
  `docker system prune`, but received exit 0. The completed matrix proves the
  extra/disabled pack cases, additional/disabled allowlist cases, OMP-to-Codex
  non-leakage, and CLI-pack precedence. A first green attempt also caught and
  removed two invalid test assertions about allowlist provenance fields that
  are not part of robot JSON; the actual decisions and agent attribution remain
  asserted.
- Isomorphism/error/resource proof: hook-mode behavior now calls the same
  helpers with no policy change; robot mode gains only the previously omitted
  selected-agent transforms. Keyword collection remains after the final pack
  set, core packs remain mandatory, and no process, I/O, retry, or hot-path
  allocation class was added.
- Verification: remote `cargo test --test robot_mode
  test_omp_robot_boundary -- --nocapture` on `vmi1264463` reported 2 passed,
  0 failed; remote `cargo clippy --test robot_mode -- -D warnings` passed;
  `cargo fmt --check` and `git diff --check` passed.
- Residual: per-call cwd, persistent history, and bridge dialect remain bounded
  missions 4, 22, and 6 respectively.

## Pass 4 record: effective Bash cwd propagation

- Fresh agent: `omp_pass_04_call_cwd`.
- Finding: the generated `tool_call` handler ignored both the structured Bash
  `cwd` and OMP's conservative leading-`cd` normalization, so dcg loaded project
  configuration and allowlists from OMP's ambient directory rather than the
  directory in which Bash would execute.
- Change: the extension consumes callback `ctx.cwd`, reuses OMP's current public
  `resolveToCwd` and `extractLeadingCdTarget` exports, applies OMP's precedence
  `structured cwd -> leading cd -> session cwd`, and passes the result through
  Bun's direct-spawn `cwd`. Filesystem-cwd resolution errors return an explicit
  block instead of evaluating under unrelated policy.
- Mutation-sensitive evidence: the intended generated-source test first failed
  specifically because the resolver import/wiring was absent; an earlier
  zero-test `--exact` invocation was rejected as evidence. The real-binary
  fixture holds the command constant while changing only the child cwd: it is
  allowed in the session directory and denied as `containers.docker` in a
  tool directory whose enforcement-only `.dcg.toml` enables that pack.
- Isomorphism/error/resource proof: command bytes, agent attribution, dialect,
  stdio framing, and direct no-shell spawn are unchanged. The new work is two
  exported OMP helper calls and one spawn option per Bash invocation; invalid or
  unrepresentable cwd state blocks with a diagnostic. No filesystem mutation or
  extra subprocess was added.
- Verification: remote unit filter on `vmi1264463` reported 1 passed, 0 failed,
  3708 filtered; remote `test_omp_robot_boundary_uses_execution_cwd_project_policy`
  reported 1 passed, 0 failed, 23 filtered; an installed-package Bun probe
  imported both public helpers and demonstrated relative, root-alias, and
  leading-`cd` outputs; `cargo fmt --check` and `git diff --check` passed.
- Residual: OMP expands internal protocol cwd values later using session state
  unavailable to this callback, so the bridge deliberately blocks those rather
  than approximating. Later extension handlers can also replace tool input
  without replaying earlier handlers; that is an upstream ordering trust
  boundary for pass 48. Full generated-TypeScript execution remains pass 10.

## Pass 5 record: fail-closed profile-state validation

- Fresh agent: `omp_pass_05_profile_validity`.
- Finding: invalid or non-Unicode winning profile values silently collapsed to
  the default profile, allowing `dcg install --omp` to succeed and write an
  extension even though OMP rejects startup. Doctor could also omit its OMP
  check entirely when an invalid legacy `PI_PROFILE` was the only OMP signal.
- Change: profile resolution is now an explicit `Absent` / `Default` / `Named`
  state machine with fallible validation. Absence consults `PI_PROFILE`; empty,
  whitespace, literal `default`, and valid named `OMP_PROFILE` values suppress
  the legacy value; invalid grammar, reserved device names, and non-Unicode
  winning values fail before either project or user/profile installation can
  write. Both doctor renderers surface the same error, and `PI_PROFILE`
  presence participates in OMP-use detection.
- Mutation-sensitive evidence: the pre-fix real-CLI test expected invalid
  `OMP_PROFILE=..` to fail but observed success and exit 0. The final matrix
  proves valid modern profile plus invalid losing legacy, empty/default modern
  profile plus invalid losing legacy, absent modern plus valid legacy, invalid
  modern plus valid legacy, invalid legacy alone, and Unix non-Unicode modern
  input. Every rejection case fences the default, named fallback, explicit
  agent-dir, and project destinations against writes; doctor preserves an
  existing project marker byte-for-byte while reporting the invalid winner.
- Isomorphism/error/resource proof: valid profile paths and the documented
  presence-based precedence are unchanged. Only states OMP's CLI refuses now
  return `InvalidInput`/`InvalidData`; diagnostics name the winning variable
  and escape its value. The state machine adds no subprocess, retry, filesystem
  scan, or hot bridge work.
- Verification: final-byte remote `cli_e2e profile` on `vmi1264463` reported
  5 passed, 0 failed; the exact unit contract reported 1 passed, 3708 filtered;
  remote `cargo clippy --test cli_e2e -- -D warnings` and
  `cargo clippy --lib -- -D warnings` passed; `cargo fmt --check`, targeted UBS,
  and `git diff --check` found no actionable changed-line issue.
- Source precision: current upstream OMP source at
  `989271449953548f1a399789bfc7500003115ebc` defines the replicated grammar and
  presence-based precedence. The local CLI executable reports `omp/18.0.4`,
  while the separately inspectable installed TypeScript packages report
  `17.4.2`; neither version label is substituted for the source/behavior proof.
- Residual: non-Unicode E2E is Unix-only; native Windows case and wide-string
  behavior remain bounded to pass 14. Stale profile-derived override behavior
  remains pass 12, and full all-target gates remain pass 50.

## Pass 6 record: execution-backend shell dialect

- Fresh agent: `omp_pass_06_windows_dialect`.
- Finding: the generated bridge always passed `--dialect posix`. That is exact
  for OMP's ordinary and managed-async embedded Brush routes, including native
  Windows, but not for a synchronous UI-capable `pty: true` call: OMP executes
  that call through its configured external shell, which may be Cmd or
  PowerShell and therefore interprets caret/backtick evasions differently.
- Change: POSIX remains the zero-lookup default. Only the same eligible local
  PTY predicate OMP uses (`pty: true`, not async, UI present, and
  `PI_NO_PTY != 1`) resolves the configured shell and maps OMP's own public
  classifiers to `cmd`, `ps`, or `posix`. Resolution failure returns an
  explicit block. Documentation now describes both the route table and the
  deliberately unresolved ACP terminal case.
- Fresh-eyes correction: a direct `@oh-my-pi/pi-utils/procmgr` import is legal
  under the package's source exports but is not retained by OMP's compiled
  single-binary loader, which excludes root catch-all wildcard subpaths. The
  final bridge imports the retained root `procmgr` namespace instead. The
  `config/settings` import remains on an explicitly retained `config/*`
  surface, while the broad coding-agent root remains type-only.
- Mutation-sensitive evidence: the exact source-contract test failed remotely
  on the hard-coded bridge (1 run, 0 passed) at the missing dynamic import and
  predicate. On final bytes, remote job `j-29990583984455876` on
  `vmi1264463` reported 1 passed, 0 failed, 3709 filtered. An installed
  classifier probe identified Cmd, Windows PowerShell, pwsh, and Bash as the
  expected dialect families. A zero-test red and a later local-fallback
  dialect-matrix attempt were explicitly rejected as evidence.
- Isomorphism/error/resource proof: ordinary, async, non-UI, and `PI_NO_PTY=1`
  commands retain their exact POSIX analysis without a settings lookup. The
  rare external-PTY route adds one cached settings read and two upstream
  classifiers; no shell or extra process is introduced. Unknown shell names
  follow OMP's own POSIX-argument fallback instead of triggering all-dialect
  replay and false positives.
- Verification: final `cargo fmt --check`, `git diff --check`, and status were
  clean. The established evaluator regressions already pin PowerShell/Cmd
  escape positives and POSIX literal negatives; their attempted pass-local RCH
  reruns were not credited because one fell back locally and its remote retry
  was intentionally interrupted after the final exact contract had completed.
- Shared-tree note: a concurrent actor published the implementation as
  `f0354a3` and the compiled-loader correction as `ae09248` while the fresh
  agent was still verifying. The primary agent preserved those commits and did
  not rewrite published history.
- Residual: OMP's public ExtensionContext exposes neither ACP terminal
  capability nor selected Bash backend, and ACP and JSON-RPC both report
  `mode: "rpc"`. Guessing would overmatch POSIX-only JSON-RPC calls, so non-PTY
  RPC stays POSIX-scoped until upstream exposes the actual route. Full generated
  TypeScript execution remains pass 10; the API boundary returns in pass 48.

## Pass 7 record: monotonic child transition automaton

- Fresh agent: `omp_pass_07_transition_table`.
- Finding: the bridge returned immediately on exit 0 or 2 before parsing
  stdout, so a machine-readable `deny`, `ask`, or `indeterminate` verdict on
  either status was silently erased. Child status and data were two competing
  decision channels without an explicit precedence contract.
- Change: generated TypeScript now contains a pure exported classifier and a
  literal 5 outcome x 7 parsed-verdict transition table. The invariant is
  `block iff parsed deny-like verdict or exit 1`. Exit 0/2 otherwise allows;
  spawn failure and other status otherwise remain the documented visible
  infrastructure fail-open. JSON parsing distinguishes empty, malformed,
  allow, deny, ask, indeterminate, and unknown; reason/rule metadata is kept
  only when it is actually a string.
- Fresh-eyes correction: an initial 1 MiB JSON parse cap was removed. Stdout had
  already been fully buffered, so the cap provided no resource bound and could
  instead reclassify a valid large deny as malformed, violating monotonicity.
  A ceremonial spawn-catch classifier call was also removed; the spawn row
  remains an explicit total replay/model specification with its reachability
  documented.
- Mutation-sensitive evidence: the pre-change source witness was the status-only
  `if (exitCode === 0 || exitCode === 2) return;` before JSON parsing. The new
  Rust contract pins every literal cell and rejects that early return, the
  pseudo-cap, missing pure classifier export, and untyped reason/rule fields.
- Isomorphism/error/resource proof: all normal robot pairs retain their prior
  behavior (`0+allow`, `1+deny/ask/indeterminate`, and `2+warn`). Contradictory
  pairs now resolve monotonically toward safety; malformed/empty exit 1 still
  blocks; status-only infrastructure faults still diagnose and fail open. The
  table is fixed module data, adds no process/I/O/retry, and classification is
  one parse plus two constant-key lookups.
- Verification boundary: final `cargo fmt --check` and `git diff --check`
  passed. Strict RCH attempts could not obtain an admissible worker: the first
  offered `[RCH] local` and was interrupted immediately; later attempts exited
  103 for pressure/slot/preflight constraints. Therefore no targeted Rust
  execution or clippy result is claimed for final bytes in this pass. Pass 10
  owns executable generated-TypeScript replay, and pass 50 must clear the
  pending Rust/clippy gates.
- Shared-tree note: concurrent commits published the table (`06ad9a6`), its
  handler integration (`ae671fc`), and the fresh-eyes cap removal (`a1bcae0`)
  while the delegated agent worked. Published history was preserved.
- Residual: stdout/stderr channel framing is pass 8; process deadline/crash
  behavior is passes 21/34; large-command and control-byte behavior is pass 31.

## Pass 8 record: stdout decision authority and stderr visibility

- Fresh agent: `omp_pass_08_stdio_framing`.
- Finding: the bridge drained both child streams concurrently, but returned
  from both allow and block paths before exposing nonempty stderr. Only an
  infrastructure outcome surfaced it, so useful dcg diagnostics disappeared
  during the two normal protocol outcomes.
- Change: stdout remains the sole input to the pure child classifier. Stderr is
  trimmed separately, included exactly once in an infrastructure diagnostic,
  or emitted exactly once through a shared prefixed diagnostic point before
  either a normal allow or block return. The generated source documents these
  streams as distinct capabilities.
- Mutation-sensitive evidence: before the production edit, the focused source
  witness exited 1 because the required shared stderr route did not exist. The
  final contract pins the sole stdout-fed classifier call, proves the pure
  classifier contains no stderr dependency, orders the shared diagnostic before
  both non-infrastructure returns, counts both emission sites, and retains the
  all-blocking exit-1 row. Thus stderr-shaped JSON on exit 0 remains diagnostic
  rather than fabricating a block, while an empty/swapped stdout on exit 1
  cannot silently allow the command.
- Isomorphism/error/resource proof: parsed decisions, status precedence, spawn
  behavior, process count, buffering, and command bytes are unchanged. The only
  added hot-path work is trimming an already-buffered stderr string and a
  conditional; empty stderr remains silent. Nonempty diagnostics become visible
  once without acquiring decision authority.
- Verification boundary: `cargo fmt --check` and `git diff --check` passed on
  final bytes. Three strict RCH attempts each offered a local fallback because
  all workers were inadmissible under pressure/slot/preflight constraints and
  were immediately interrupted. Bounded UBS also did not finish. No Rust test,
  clippy, or UBS green is claimed; passes 10 and 50 retain those gates.
- Shared-tree note: a concurrent actor published the red contract (`2b20a2c`),
  production repair (`522f447`), and generated capability comment (`ba55a22`)
  while the fresh agent worked. The remaining strengthening assertions and this
  record are landed without rewriting that published history.
- Residual: executable stream replay belongs to pass 10; deadline/stalled-pipe
  behavior remains passes 21 and 34.

## Pass 9 record: total tool-call command-shape boundary

- Fresh agent: `omp_pass_09_tool_shape`.
- Finding: the generated callback silently returned for an identifiable Bash
  event whose input was not an object or whose required `command` was missing or
  non-string. OMP's ordinary agent loop validates this schema before emitting
  `tool_call`, but its extension machinery also has non-loop dispatch seams; a
  safety boundary should not interpret malformed Bash input as consent.
- Change: generated TypeScript exports a pure three-state classifier for
  `other-tool`, `invalid-bash`, and valid `bash`. Other tools remain untouched;
  invalid Bash shape returns an explicit block; an empty primitive string stays
  OMP's schema-valid no-op; every nonempty primitive string, including only
  whitespace, proceeds byte-for-byte to dcg.
- Upstream evidence: current OMP `main` at
  `989271449953548f1a399789bfc7500003115ebc` defines Bash `command` as a
  primitive string without a minimum length, validates arguments before the
  agent-loop `beforeToolCall`, and emits the exact lower-case tool name with a
  normalized record input.
- Mutation-sensitive evidence: the pre-production source probe failed because
  the total classifier and explicit malformed-input block were absent. The
  final contract pins object/array/null rejection, exact tool-name isolation,
  required primitive-string validation, the empty-only exemption, lack of any
  trimming exemption, and classification before child spawn. The pure export is
  reserved for pass-10 executable replay.
- Isomorphism/error/resource proof: valid nonempty Bash commands retain their
  bytes and downstream path/dialect/protocol behavior; other tools and empty
  commands retain their prior no-op behavior. Only previously malformed,
  identifiable Bash events change from silent allow to a deterministic block.
  The boundary adds fixed type checks and no I/O, process, allocation class, or
  retry.
- Verification boundary: `cargo fmt --check` and committed `git diff --check`
  passed. Two strict RCH attempts offered local fallback because no worker was
  admissible and were immediately interrupted; bounded UBS did not finish. No
  targeted Rust or UBS green is claimed, and pass 10/50 retains those gates.
- Shared-tree note: a concurrent actor published the complete implementation
  and test as `fe2cb7b` while the fresh agent verified it; published history was
  preserved.
- Residual: this deliberately validates the event/object/required-command seam,
  not every optional Bash field. Other extensions can mutate the shared input;
  non-boolean `pty`/`async` can diverge from guard routing, and `env` can affect
  shell expansion. Passes 28 and 48 own those ambient-authority/composition
  boundaries rather than concealing them in this pass.

## Pass 10 record: executable generated-bridge replay certificate

- Fresh agent: `omp_pass_10_executable_bridge`.
- Finding: passes 7--9 had mutation-sensitive Rust source contracts, but no gate
  actually loaded the emitted TypeScript, exercised its exported classifiers,
  and drove the registered callback under a JavaScript runtime. A textual
  contract can remain green while generated syntax, imports, or runtime value
  semantics are broken.
- Change: an opt-in ignored Rust test writes the exact generated extension plus
  four minimal imported-API stubs, then runs a deterministic Bun harness over
  14 tool-call shapes, every cell of the 5 x 7 child transition table, and 10
  callback stream/status cases. Its certificate identifies Bun, exact case
  IDs, the embedded dcg executable, stub set, source SHA-256, and terminal
  `probeComplete`. A second run mutates the exit-0 + deny cell to allow and must
  fail at that exact case.
- Fresh-eyes correction: the first real remote compile found that sha2 0.11's
  digest array does not implement `LowerHex`; the original
  `format!("{:x}", digest)` therefore failed with E0277. Commit `0120da8`
  replaced it with the crate's established allocation-bounded byte-wise hex
  encoding. This was a genuine defect that source inspection and formatting
  did not reveal.
- Isomorphism/error/resource proof: production bridge bytes and runtime behavior
  are unchanged. The certificate is ignored by default, creates only bounded
  temporary fixtures when explicitly selected, and uses the existing generated
  source and current dcg executable. The exact corpus is finite, names every
  case, rejects incomplete execution, and makes one decision-cell mutation
  observably red.
- Verification: corrected `src/cli.rs` SHA-256
  `dce91347c78b5aeebfea2544870946276610e6f1dd482cdfe52ff0a4de41a1a3`
  compiled on Linux x86-64/glibc 2.42 with nightly 2026-06-06. The linked
  394,929,496-byte test executable SHA-256 was
  `4f81d0073bdcc79107de24551096ecfdb4c88848965ed6e77ddc15dbca3516f2`
  before and after both transport hops. On Linux x86-64/glibc 2.43 with Bun
  1.4.0, the exact ignored test reported 1 passed, 0 failed, 3718 filtered in
  0.20s; the test includes the required mutant-red run. `cargo fmt --check` and
  `git diff --check` passed on the committed bytes.
- Infrastructure boundary: RCH selected a Rust-capable worker without Bun and
  an earlier worker ran out of memory before linking. The corrected source was
  copied to a new, non-overwriting staging root, compiled against the quiescent
  warmed cache on a 29 GiB worker, and only the checksum-fenced executable was
  relayed to the Bun host. Reused build-script metadata warned that no Git
  repository was present in staging; this library-test certificate does not
  claim release build metadata.
- Residual: the four stubs deliberately model only the imported API surface.
  This proves generated-source translation and bridge semantics under Bun, not
  installation or execution inside a real OMP process; the release-blocking
  harness and full cross-seam closure remain pass 50.

## Pass 11 record: hermetic installer/uninstaller capability fence

- Fresh agent: `omp_pass_11_hermetic_env`.
- Findings: the shared Bats setup changed HOME and PATH but inherited
  `OMP_PROFILE`, `PI_PROFILE`, `PI_CONFIG_DIR`, and `PI_CODING_AGENT_DIR`.
  Those selectors can redirect OMP reads and deletion outside the fixture.
  Independently, the setup retained the live repository cwd even though OMP
  project extensions are cwd-local, so every full-uninstaller test could probe
  a real `.omp/extensions/dcg-guard.ts`.
- Change: Unix setup now preserves exact setness and values, scrubs all four
  selectors before subject functions load, binds cwd to a new `TEST_WORKDIR`,
  and restores cwd before bounded cleanup. It refuses cleanup if cwd restoration
  fails, restores the harness PATH before invoking cleanup tools, and restores
  the original selector state. The three PowerShell fixtures that directly or
  indirectly exercise OMP detection/uninstall seed synthetic non-vacuous values,
  scrub all selectors before dot-sourcing subject bytes, hard-fail if any name
  remains, and restore originals in an outer `finally`.
- Mutation-sensitive evidence: the Unix canary launches a nested fixture with
  all four hostile selectors and marker-owned exact-byte files in an external
  agent directory and external cwd. Final bytes reported 1 test, 0 failures.
  A separate temporary copy with only the `PI_CODING_AGENT_DIR` scrub removed
  reported 1 test, 1 failure at the authoritative status assertion. The
  tracked tree was never mutated for this red run.
- Fresh-eyes corrections: review first found that the nested shell lacked
  `errexit`, allowing a failed scrub assertion to be masked by a later compare;
  its EXIT trap now preserves the original status. The first full Bats run then
  found teardown could lose `rm` when a test intentionally replaced PATH;
  commit `33392ea` restores PATH before cleanup. Finally, executable PowerShell
  found that .NET `SetEnvironmentVariable(name, null, Process)` left names
  visible through the Unix `Env:` provider. Commit `4ab6326` uses
  module-qualified provider-native `Set-Item`/`Remove-Item`, preserving empty
  versus absent originals.
- Isomorphism/error/resource proof: production installer and uninstaller bytes
  are unchanged. Tests can opt back into one selector explicitly after setup;
  all normal fixture paths remain bounded under their fresh temp root. The
  fence adds only constant-size environment bookkeeping and one cwd change per
  test, with restoration failures made visible instead of silently continuing.
- Verification: Bats 1.13.0 on an idle Linux worker ran five disjoint shards
  totaling 219 cases. Results were 215 pass, 3 expected pwsh-unavailable skips,
  and one permission case invalid under root; that exact case passed as the
  unprivileged `ubuntu` user, yielding 216 behavior passes plus 3 skips. The
  OMP/Posit/Hermes/OpenCode shard was 50/50 green. On macOS PowerShell, the full
  detection, indirect Posit, and uninstall parity scripts all exited 0, including
  every OMP cleanup/failure fixture. `bash -n`, `shellcheck`, all three
  PowerShell parser checks, `cargo fmt --check`, and `git diff --check` passed.
- Shared-tree note: an automatic concurrent commit combined the initial test
  bytes with an unrelated Databricks edit in `dc6588e`; no peer byte was
  reverted or rewritten. The two executable fresh-eyes repairs landed in
  scoped follow-up commits `33392ea` and `4ab6326`.
- Residual: PowerShell execution here used pwsh on macOS, not native Windows;
  native profile/case/enumeration parity remains explicitly owned by passes 14,
  19, 40, and the release-blocking pass 50.

## Pass 12 record: provenance-sensitive stale agent-dir suppression

- Fresh agent: `omp_pass_12_stale_agent_dir`.
- Finding: current OMP suppresses an inherited `PI_CODING_AGENT_DIR` when a
  higher-priority `OMP_PROFILE` explicitly selects the default profile and the
  override is exactly the agent directory derived from a valid losing
  `PI_PROFILE`. dcg instead treated that stale child-process value as a custom
  override and installed into the inactive named profile.
- Upstream evidence: OMP `main` at
  `969a94c1eeccb1b7528cd5621934bca1908ab622` defines
  `resolvePreProfileAgentDir`/`isProfileDerivedAgentDir` as an exact-string
  provenance recognizer and has an executable fixture for both empty and
  `default` canonical selectors. Invalid losing profiles are read safely and
  provide no provenance.
- Change: Rust, the Unix installer status resolver, the Unix uninstaller's
  active resolver, and PowerShell uninstallation now derive the candidate from
  the current config root plus a valid losing legacy profile and suppress only
  an ordinal exact match. Named active profiles still ignore the legacy
  override; genuine custom, sibling, child, case-different, dot-component,
  repeated-separator, and trailing-separator spellings remain overrides. The
  uninstaller retains its separate raw override cleanup candidate as bounded
  defense in depth.
- Fresh-eyes corrections: review rejected Rust `Path` equality because it can
  collapse syntax that upstream's JavaScript string equality distinguishes;
  production now compares `OsStr` values directly and tests compare raw OS
  strings. Positive Rust/E2E fixtures were rebuilt with component-wise joins so
  native Windows separators match production. Review also restored the raw
  uninstall cleanup candidate after an intermediate version accidentally tied
  it to the suppressed active value. Finally, an `--exact` integration command
  that ran zero tests was rejected and rerun with the harness-qualified filter.
- Mutation-sensitive evidence: exact current Unix bytes on the idle vmi129 host
  passed both focused cases. The same new decision-table test against the exact
  historical pre-fix `2c280aa:install.sh` (SHA-256
  `c3f2b85f138d77996979ef0c3104bf6d530d884c407511a876677a5bd7204b85`)
  exited 1 at the first stale-path assertion. This proves the positive is not
  satisfied by setup, a generic default, or a path-shaped custom override.
- Isomorphism/error/resource proof: behavior changes only for the conjunction
  `default active profile + valid losing PI_PROFILE + exact derived override`.
  Every other resolver state preserves its former target. The recognizer adds
  fixed validation, one bounded path derivation, and one ordinal comparison;
  it adds no filesystem probe, canonicalization, retry, process, or hot hook
  work. Winning invalid/non-Unicode Rust selectors still fail before a write.
- Verification: RCH on `ovh-a` ran the exact Rust unit at base `97d8563`:
  1 passed, 0 failed, 3719 filtered. RCH on `vmi1149989` ran the current
  `26e8fed` real-binary E2E: 1 passed, 0 failed, 141 filtered. Exact staged Unix
  bytes passed 2/2 focused Bats cases; Bats discovered 143 total tests. macOS
  PowerShell Tests 0--18 all passed, including eleven new provenance states.
  `bash -n`, ShellCheck, PowerShell parsing, `cargo fmt --check`, and
  `git diff --check` passed. A strict RCH attempt that offered local fallback
  was interrupted immediately and is not credited.
- Shared-tree note: automatic concurrent commits published Rust/E2E bytes with
  an unrelated broad `Cargo.lock` refresh in `2c280aa`, shell production plus
  initial Bats bytes in `97d8563`, and the remaining decision table plus
  formatting-only Rust changes in `26e8fed`. Published peer history was
  preserved; `main` and its legacy mirror were synchronized after each observed
  push.
- Residual: PowerShell ran under macOS pwsh, not a native Windows host. Native
  case semantics and cross-language path parity remain passes 14 and 40; the
  unrelated lockfile refresh receives the full locked build/test gates in pass
  50 rather than being misattributed to this resolver change.

## Pass 13 record: exact OMP config-root normalization and filesystem semantics

- Fresh agent: `omp_pass_13_config_normalization`.
- Finding: dcg concatenated `PI_CONFIG_DIR` without Node's lexical path
  normalization. Dot/parent components could therefore send installation and
  cleanup to paths OMP never loads, and pass-12's exact stale-agent provenance
  recognizer could misclassify OMP's normalized derived value as a custom
  override. PowerShell additionally treated a leading backslash as a separator
  on POSIX, despite Node POSIX treating it as a literal filename byte.
- Upstream oracle: OMP `main` at
  `969a94c1eeccb1b7528cd5621934bca1908ab622` joins the home directory and
  `PI_CONFIG_DIR || ".omp"` before composing `agent`/profile descendants.
  Executable Node oracles covered empty/default, both leading-separator classes,
  repeated separators, `.`, `..`, excess-parent root clamping, trailing
  separators, POSIX drive-looking names, and Windows drive-qualified rejection.
- Change: Rust and both Bash lifecycle scripts now normalize the complete joined
  config root lexically without invoking Node. PowerShell uses platform-selective
  separator trimming, `Path.Combine`/`GetFullPath`, and exact `System.IO`
  operations for OMP detection, enumeration, reads, and POSIX deletion. Native
  Windows deletion retains provider-qualified `Remove-Item -Force`, preserving
  read-only generated-file cleanup where Windows provider separators are the
  correct semantics. Normalized roots also feed pass-12 stale provenance.
- Fresh-eyes corrections and mutation evidence: the first full PowerShell
  detection run failed only the POSIX leading-backslash assertion, proving the
  original `Join-Path` defect. Replacing only path composition still made the
  new cleanup test delete both separator-normalized decoys because PowerShell's
  filesystem provider reinterpreted the path even with `-LiteralPath`; the
  exact `System.IO` boundary made that second live red green. Final review then
  caught loss of `Remove-Item -Force` for Windows read-only files and restored
  it behind a native-Windows branch with a CI-owned fixture. Separately, the
  normalized-stale Bash test failed against exact pre-pass-13 installer bytes.
- Isomorphism/error/resource proof: already-normalized ordinary paths retain
  their destinations. Only spellings for which Node's join changes the lexical
  result, or PowerShell would reinterpret a POSIX backslash, change behavior.
  The Bash fold is linear in the bounded environment value; Rust uses native
  path components; PowerShell adds no subprocess or filesystem canonicalization.
  Missing candidates remain skips, while permission/I/O failures stay explicit
  failures. Marker ownership remains mandatory before deletion.
- Verification: strict RCH on `ovh-a` ran the focused Rust oracle slice with
  2 passed, 0 failed, 3719 filtered. Strict RCH on `vmi1149989` ran the real CLI
  normalized-stale fixture with 1 passed, 0 failed, 141 filtered. Five focused
  Bats cases passed on exact staged bytes. Full macOS PowerShell detection and
  uninstall suites exited 0, including literal-root positive/decoy-negative
  detection, the 14-row Node path table, and five exact cleanup assertions.
  Remote `cargo check --all-targets` and all-target clippy with warnings denied,
  local `cargo fmt --check`, Bash syntax, ShellCheck, PowerShell
  parse-load, and `git diff --check` passed.
- Shared-tree note: automatic concurrent commits published the main
  implementation as `0dd6616`, its formatting repair as `3bea88c`, and the
  PowerShell exact-filesystem boundary as `af8c3fb`. An unrelated release-only
  version bump `f4cc5ab` landed between them and was preserved. The scoped
  Windows force-parity correction is `03205e0`.
- Residual: the remote Windows cross-check attempt was infrastructure-red because
  that worker lacked `x86_64-pc-windows-gnu`; it is not credited as code
  evidence. Native Windows execution of the decision table and read-only fixture
  remains CI-owned and is revisited in passes 14, 37, 40, and 50.

## Pass 14 record: case-exact PowerShell profile-state parity

- Fresh agent: `omp_pass_14_profile_case`.
- Finding: PowerShell's active-profile resolver used case-insensitive `-match`
  and `-ne`, so `Work` could masquerade as a valid named profile even though
  OMP and dcg's Rust/Bash resolvers require lowercase ASCII. Cleanup would then
  ignore a genuine custom `PI_CODING_AGENT_DIR` and route to a profile OMP
  rejects, with different collision behavior across case-sensitive and
  case-insensitive filesystems.
- Upstream oracle: pinned OMP `dirs.ts` trims the winning value; maps empty,
  whitespace, and exact lowercase `default` to default mode; accepts only
  `^[a-z0-9][a-z0-9._-]{0,63}$` subject to trailing-dot and reserved-device
  checks; and selects `OMP_PROFILE` by presence before considering legacy
  `PI_PROFILE`. An invalid modern winner never falls through to legacy state.
- Change: only PowerShell's main profile grammar/sentinel comparisons changed to
  `-cmatch` and `-cne`. Existing case-insensitive reserved-device rejection,
  pass-12 ordinal stale-provenance comparison, pass-13 normalized paths, and all
  marker/enumeration/deletion behavior remain unchanged. A 14-row executable
  table freezes absent, empty, whitespace, lowercase/uppercase default, valid
  lowercase, invalid uppercase, modern/legacy presence, custom overrides, and
  exact stale-derived suppression.
- Mutation-sensitive evidence: with the new table and exact pre-fix production
  bytes, the full suite exited 1 with exactly two failures. Legacy
  `PI_PROFILE=Work` and winning `OMP_PROFILE=Work` both incorrectly selected
  `.custom-omp/profiles/Work/agent` instead of preserving the custom override;
  the other twelve rows passed. Exact commit `8987923` made both rows green.
- Isomorphism/error/resource proof: lowercase valid names and exact lowercase
  default states retain identical paths. Only uppercase/mixed-case spellings
  stop being accepted as named profiles; presence and invalid-winner non-fallback
  remain intact. The change is two scalar comparison operators on uninstaller
  control flow and adds no I/O, allocation class, process, retry, or hot-hook
  work.
- Verification: the exact-commit full PowerShell uninstall suite exited 0 for
  labeled Tests 0--19 and all 14 new rows. The complete detection suite and four
  PowerShell parse-load checks exited 0. `uninstall.ps1` SHA-256 was
  `b4d45b4f872e62b04684c3228393557a6b2a38a38ea55504f079d03be39769df`;
  its test SHA-256 was
  `5ce19cd14743a90a571e9dec69040b90601821a16c2f729bcec185cdf1205f2f`.
  Exact clean-overlay remote `cargo check --all-targets` at base `8987923`
  passed; `cargo fmt --check` and `git diff --check` passed. The Rust/Cargo
  inputs are byte-identical to pass 13's all-target clippy proof; a redundant
  cold clean-overlay clippy run was stopped and is not credited. UBS exited 0
  but recognized no PowerShell language and is explicitly not credited as
  analysis evidence.
- Shared-tree note: the automatic concurrent actor committed and pushed the
  complete two-file change as `8987923`; the published bytes were preserved.
- Residual: executable PowerShell evidence is macOS pwsh, not native Windows
  PowerShell 5.1. The native Windows table remains CI-owned and is revisited in
  passes 37, 40, and 50.

## Pass 15 record: exact OMP extension health and bounded repair

- Fresh agents: `omp_pass_15_extension_health` was interrupted before a usable
  result; the independent retry `omp_pass_15_extension_health_retry` found and
  closed the structured-output contamination seam. The primary agent reviewed
  the complete resulting diff and performed a second fresh-eyes state-machine
  audit before acceptance.
- Finding: doctor treated the presence of the ownership marker as proof that an
  OMP extension was usable. A truncated, stale, syntactically malformed, or
  handler-disabled generated file could therefore be reported healthy even
  though OMP bash calls were unguarded. OMP also loads both the cwd project
  extension and the active-profile user extension, so a current peer must not
  hide an unhealthy dcg-owned copy that will register another callback.
- Change: inspection now has four explicit states: absent, foreign,
  dcg-owned-unhealthy, and exact-current. Ownership remains a UTF-8 marker
  decision governing overwrite authority; health requires byte equality with
  the extension source generated for the running dcg executable. Registration
  inspects both loaded scopes and gives either unhealthy owned copy precedence
  over a healthy peer. `doctor --fix` refreshes at most the two authoritative
  scopes, re-inspecting after every atomic write, and only reports success once
  all dcg-owned loaded copies are exact-current. Foreign and invalid-UTF-8 files
  remain untouched.
- Fresh-eyes corrections: the two-scope repair initially stopped after the
  first stale file; it now repairs both in one invocation. An invalid-UTF-8
  marker-shaped file was added as a negative ownership fixture. The structured
  doctor path initially allowed the installer to print human progress before
  its JSON document; an explicit `announce` control now keeps ordinary install
  and pretty doctor output while JSON doctor emits exactly one parseable
  document. Final review removed a superseded dead helper and corrected the
  verification test to respect the schema's intentional omission of false
  `fixed` fields.
- Mutation/evidence boundary: an exact historical pre-fix remote mutation run
  was admitted and compiled, but the wrapper lost its terminal test result; it
  is explicitly not credited as red evidence. Final behavioral binaries passed
  all five `omp_extension` cases (0 failed) and the focused structured-doctor
  repair case (0 failed). Those behavior runs used the available locally built
  nightly test artifact and are functional, not release or timing evidence.
- Compiler/lint verification: strict clean-overlay RCH
  `cargo check --all-targets` passed on `vmi1149989`, receipt base `6f87d30`
  with overlay
  fingerprint
  `512dc5b9c33dcf81ee18d3a9e216d1d49f6d2dac7c835ce95193c20e62462a5a`.
  The worker then rejected clippy because its pinned toolchain lacked that
  component; this infrastructure failure is not credited. The exact final
  bytes passed local pinned-nightly
  `cargo clippy -j 2 --all-targets -- -D warnings`, plus `cargo fmt --check`
  and `git diff --check`.
- Exact final source hashes: `src/cli.rs`
  `5d9340182dd9f8a509152901935531040c988187d31e1b83a6b34fa1c5dc6d1a`;
  `tests/cli_e2e.rs`
  `38d8ef7e70038881cca34e61477db531b3c55c0ad3c46ab8bce20432523dcf6a`.
  The production health work is doctor/install-only: it adds no hook hot-path
  read, allocation, regex, process, retry, or callback work, so no performance
  improvement is claimed for this correctness pass.
- Shared-tree note: the automatic concurrent actor published intermediate
  pass-15 bytes across `93ca67b`, `352445b`, `7f566ac`, `d0794d1`, `87de497`,
  `b6e4867`, `479dd35`, `9754712`, and `6f87d30` using unrelated generic commit
  subjects. The final cleanup is `eee534c`; all published peer history was
  preserved.
- Residual: exact generated bytes cannot prove that OMP's merged
  `disabledExtensions` configuration enables the extension. Implementing only
  a partial settings parser would create a new false-green boundary, so merged
  configuration truth is deferred to a later dedicated pass. Two exact-current
  copies also mean duplicate callbacks; that lifecycle/performance question is
  retained for passes 26 and 48 rather than changing semantics without a trace.

## Pass 16 record: case-exact PowerShell ownership authority

- Fresh agent: `omp_pass_16_marker_case`.
- Finding: `Unconfigure-OmpExtension` used PowerShell's ordinary `-notmatch`
  operator. PowerShell string comparison operators are case-insensitive unless
  their explicit `c` form is selected, so a user-authored file containing
  `DCG-OMP-EXTENSION` was incorrectly granted dcg ownership and deleted. Rust's
  UTF-8 substring check and Unix grep path both require the lowercase marker,
  making Windows the dangerous outlier.
- Change: the uninstaller now searches for the lowercase ASCII marker with
  `String.IndexOf(..., StringComparison.Ordinal)`. The switch is compatible
  with Windows PowerShell 5.1 and makes the authority decision ordinal without
  changing its established substring scope. Tests retain the exact lowercase
  positive and add uppercase, one-character, and punctuation near-markers;
  every negative asserts false return, file survival, and byte preservation.
- Mutation-sensitive evidence: direct macOS PowerShell evaluation of the
  removed predicate showed that uppercase `DCG-OMP-EXTENSION` was treated as a
  match. The replacement ordinal matrix accepts the exact lowercase marker and
  rejects all three near-markers. Both changed PowerShell files parsed through
  `ScriptBlock.Create`, and `git diff --check` passed. PSScriptAnalyzer was not
  installed; UBS recognized no PowerShell language, so neither is credited.
- Fresh-eyes correction: the initial production comment called the comparison
  a byte-spelling contract even though `ReadAllText` returns a .NET string. It
  now accurately calls the marker an exact case-sensitive ASCII spelling. Two
  additional primary-agent PowerShell invocations were blocked before execution
  by dcg's embedded-launcher policy; no bypass was attempted and those blocked
  commands are not credited as tests.
- Safety/resource proof: the change only narrows deletion authority. Path
  discovery, bounded enumeration, read/delete error reporting, exact lowercase
  generated-file cleanup, and every runtime hook path are unchanged. The new
  operation is a linear ordinal substring search over the already-read bounded
  extension text, replacing a linear regex search and adding no I/O, process,
  retry, allocation class, or hot-hook work. No performance gain is claimed.
- Exact final hashes: `uninstall.ps1`
  `bb8d78921e97d8d509ab23df905e4271e5008f290ef004c91e13ffb5cb10e4a1`;
  `tests/installer/uninstall_test.ps1`
  `1de8a66555fa3dc78a549e5c6a413d062f53892cd849066b13de20aa206d7b60`.
- Verification boundary: the full uninstaller test intentionally deletes its
  temporary fixture trees. It was not rerun without the user's explicit file
  deletion permission. Native Windows PowerShell 5.1 execution also remains
  CI-owned; this pass's executable semantics evidence is macOS `pwsh` only.

## Pass 17 record: complete Unix OMP pre-confirmation inventory

- Fresh agent: `omp_pass_17_unix_inventory`.
- Finding: `main` always ran OMP cleanup after consent but never included OMP
  in its pre-confirmation inventory. With an OMP extension as the only dcg
  artifact, the Unix uninstaller printed “Nothing to remove” and returned
  before reaching cleanup. Merely probing the active/default file would still
  miss inactive-profile, raw override, custom-config-root, and cwd project
  extensions that the cleanup authority can remove.
- Change: `collect_omp_uninstall_extensions` is now the single candidate
  authority shared by inventory and cleanup. It preserves the exact existing
  active/default/current roots, both inactive-profile roots, raw
  `PI_CODING_AGENT_DIR`, and cwd-only project candidates. A read-only inspector
  deduplicates those paths, applies the same case-sensitive ownership marker,
  and returns distinct none/found/incomplete states while retaining paths in an
  array rather than flattening newline-bearing names. The main inventory lists
  every owned path; incomplete enumeration is warning/actionable state and can
  no longer produce “Nothing to remove.” Cleanup deliberately re-enumerates and
  rechecks marker ownership after confirmation, preserving its TOCTOU boundary.
- Mutation-sensitive evidence: a non-deleting real-main fixture containing
  only an inactive-profile extension made exact baseline `4ff9697` print
  “Nothing to remove” with no OMP disclosure. Current bytes printed the exact
  OMP path and then stopped at the no-TTY confirmation boundary; the fixture's
  SHA-256 remained
  `3fc801ec50c72923ef69f7218577c140cfe12eda2989e23d0308e8f043d8c278`
  before and after. An uppercase near-marker negative remained undisclosed with
  SHA-256
  `79d79cc2fb58fee0ff412915fc74064166ab3d5b898eae1a800d9f5e8305b3cf`
  unchanged. A cleanup proof replaced `rm` with a recorder: the exact inactive
  marker-owned path was selected and the `removed` result emitted while the
  source file remained present.
- Tests: three Bats cases freeze complete scope disclosure/nonmutation,
  case-sensitive near-marker preservation, and truthful incomplete-profile
  enumeration. `bash -n uninstall.sh`, ShellCheck, `bats --count` (149 tests),
  and `git diff --check` passed on the exact final bytes. UBS recognized no
  relevant shell language and is not credited. The deletion-bearing Bats suite
  was not run without explicit file-deletion permission.
- Fresh-eyes/resource proof: primary review confirmed the extraction preserved
  the prior resolver, candidate order, deduplication, warnings, marker check,
  and removal result. Inventory adds one bounded O(P+C) read-only scan before
  consent; cleanup performs a fresh O(P+C) scan only after consent. This is an
  uninstall-only cold path with no runtime-hook work and no performance claim.
- Exact source hashes: baseline `uninstall.sh`
  `fb78ec45e57d045fd1f9855f798588918dd3eb5e71666f7e9a2c00f5b2677675`;
  final `uninstall.sh`
  `d6799669128951992d25a40850e7c64a69b50ee5586380e0598a69a839d790ec`;
  final `tests/install/agent_config_test.bats`
  `ceaff18b2f9796839da0278e585af1ff9c449b07ecce62b49d962fdcf8290290`.
- Residual: native macOS Bash 3.2 execution and actual deletion remain release
  gates. Marker-read and profile-enumeration warning precision is revisited in
  pass 20; duplicate candidates and cwd failure semantics are pass 18.

## Pass 18 record: bounded OpenCode project cleanup authority

- Fresh agent: `omp_pass_18_candidate_safety`.
- Scope correction: archaeology showed this adjacent lifecycle defect belonged
  to `unconfigure_opencode`, not the pass-17 OMP enumerator. OpenCode project
  installation resolves a Git root, while Unix cleanup built user, resolved
  project, and relative project candidates. At a repository root the last two
  names alias the same file. Worse, `current_repo_root` allowed a failed
  `pwd -P` to flow as empty successful output, so interpolation admitted the
  root-level candidate `/.opencode/plugins/dcg-guard.js`.
- Change: `current_repo_root` now returns nonzero with no path when physical cwd
  resolution fails. Cleanup begins with exactly one user candidate and admits
  exactly one resolved project candidate only on helper success; the relative
  alias is gone. The helper's deliberate non-Git physical-cwd fallback remains
  for bounded legacy cleanup, and every candidate still requires the exact
  `dcg-opencode-plugin` ownership marker before removal.
- Mutation-sensitive/static evidence: exact historical source has three
  candidate literals, including both absolute and relative project aliases;
  current source has two capability classes and no relative alias. A mocked-rm
  test freezes one and only one project removal attempt at repo root. A mocked
  `pwd` failure freezes nonzero/no-output behavior, which prevents construction
  of a root-level candidate. Fresh-eyes controls preserve a foreign project
  plugin byte-for-byte and preserve the non-Git physical-cwd fallback.
- Verification: `bash -n uninstall.sh`, ShellCheck, `bats --count` (153 tests),
  and `git diff --check` passed. The deletion-bearing Bats suite was not run
  without explicit file-deletion permission, so the new executable tests remain
  source/parse evidence until that gate. No Rust files changed and Cargo gates
  were not rerun.
- Fresh-eyes/resource proof: the primary review corrected a misleading
  “supported cases” comment to “existing cleanup cases,” retained both mock
  function teardowns, and strengthened foreign-file preservation from a token
  assertion to an exact snapshot comparison. The ordinary repo-root cleanup
  removes one redundant file test and marker read; all effects are confined to
  uninstall and no timing or runtime-hook performance gain is claimed.
- Exact final hashes: `uninstall.sh`
  `8b1599f7e8eb3d322ffef01a02edbe26755284daa9dba36f65293895e315af6e`;
  `tests/install/agent_config_test.bats`
  `e47409e773403343c9ce74b9ff1fc05ff821722afea948db4f9ffd117e0c0aec`.
- Shared-tree note: the automatic actor published the implementation and first
  controls as `3669a5a`, then the remaining agent controls as `39e5123`.
  Primary-agent snapshot strengthening is `cb86d66`; all history was preserved.
- Residual: cleanup still treats a marker-read error like a foreign file and
  can under-report removal failures; pass 20 owns truthful non-duplicated error
  reporting. Native Bash 3.2 and real deletion remain release gates.

## Pass 19 record: hidden and system OMP profiles on Windows

- Fresh agent: `omp_pass_19_hidden_profiles`.
- Finding: production was already correct. `Get-OmpProfileDirectories` uses the
  one-argument `[System.IO.Directory]::GetDirectories($Path)` overload. The
  official .NET source delegates that overload to `EnumerationOptions.Compatible`,
  whose `AttributesToSkip` value is zero, so Hidden and System directories are
  included. Retaining this overload also avoids imposing a newer API shape on
  the supported Windows PowerShell 5.1 path.
- Change: production behavior is unchanged; `uninstall.ps1` now documents the
  compatibility contract. Static tests pin the one-argument API, reject
  `Get-ChildItem` and `EnumerationOptions` substitutions, and pin ordinal
  ownership-marker matching. A native-Windows conditional fixture creates an
  inactive Hidden+System `work` profile containing an exact owned extension and
  a Hidden `personal` profile containing an uppercase near-marker foreign file.
  It proves both directories are enumerated, only the exact marker-owned file is
  selected for removal, the foreign file survives, and original attributes are
  restored before teardown.
- Verification: PowerShell load/parse and `git diff --check` passed on the exact
  final bytes. UBS exited successfully but is not credited as meaningful
  PowerShell analysis. The deletion-bearing full PowerShell suite was not run
  without explicit file-deletion permission, and the native-Windows fixture is
  CI-owned, so no native Windows or Windows PowerShell 5.1 runtime claim is made.
- Fresh-eyes/resource proof: enumeration remains bounded to immediate profile
  children, exact ordinal marker ownership remains the deletion authority, and
  there is no additional production I/O, recursion, allocation, or hot-path
  work. This pass therefore makes no performance claim.
- Exact final hashes: `uninstall.ps1`
  `35374b29c119162d378a6f8d6b5abce4baba62fe5acef7de2e1913db1c215a5b`;
  `tests/installer/uninstall_test.ps1`
  `7746e34eb227110a7eed9f5e476622c5be8183d6cd2d32466b57b1d7accee399`.
- Shared-tree note: the automatic actor published the compatibility contract as
  `a6a3bcd` and the native-Windows fixture as `2f30596`; history was preserved.
- Residual: the full/native Windows deletion path remains unexecuted locally.
  Pass 20 owns truthful, non-duplicated read/enumerate/delete failure reporting
  and summary success markers on Unix and Windows.

## Pass 20 record: truthful OMP uninstall failure summaries

- Fresh agent: `omp_pass_20_error_truth`.
- Finding: Unix trusted `rm -f` exit zero without verifying that the directory
  entry disappeared. A successful no-op remover could therefore forge both the
  machine `removed` marker and the human `Removed Oh My Pi extension` summary
  while the exact marker-owned file remained. PowerShell already enforced the
  equivalent file-absence postcondition and accumulated failures correctly.
- Change: Unix now requires both `! -e` and `! -L` after `rm`, covering ordinary
  entries and a remaining dangling symlink. A direct mocked-remover test freezes
  one exact path-specific warning, no raw success marker, and byte preservation;
  a second pass through `report_unconfigure` freezes the absence of the human
  success summary. PowerShell receives the matching no-op-remover inverse test
  for its already-correct production contract.
- Mutation-sensitive evidence: against exact pre-fix baseline `c8fd2f8`, a
  mocked successful no-op `rm` produced `baseline_summary_count=1`. Current
  bytes produced `current_warning_count=1` and `current_summary_count=0`, naming
  the exact failed path. The candidate hash remained
  `7d056c6e0c64ab85f795728ff0ebeef14610ab515fc4bd58aef9922d03c3dc93`
  before and after. The safe harness left two `/tmp/dcg-pass20-*.out` evidence
  files in place because file deletion was not authorized.
- Fresh-eyes/error proof: existing Unix enumeration and marker-read failures
  already set aggregate failure state, warn precisely during cleanup, and
  suppress the success marker. PowerShell config resolution, enumeration,
  reads, and deletion failures already suppress its Boolean success result.
  Candidate paths are deduplicated on both platforms. The generic inventory
  warning and later precise cleanup warning are not duplicate observations:
  they report separate pre-consent and post-consent reads around the deliberate
  TOCTOU boundary. Unix function exit zero remains the established best-effort
  reporter protocol; only the exact `removed` marker authorizes success output.
- Verification: `bash -n uninstall.sh`, ShellCheck, `bats --count` (154 tests),
  PowerShell load/parse, and `git diff --check` passed on the exact final bytes.
  Full deletion-bearing Bash/PowerShell suites were not run without explicit
  permission. Native Windows and Windows PowerShell 5.1 runtime remain CI-owned.
  No Rust changed, so Cargo gates were not rerun.
- Exact final hashes: `uninstall.sh`
  `7d056c6e0c64ab85f795728ff0ebeef14610ab515fc4bd58aef9922d03c3dc93`;
  `tests/install/agent_config_test.bats`
  `b7352671d35842a2e31973fd9156781bfb55894ae49d45d709e9e61f9d98bf55`;
  `uninstall.ps1`
  `35374b29c119162d378a6f8d6b5abce4baba62fe5acef7de2e1913db1c215a5b`;
  `tests/installer/uninstall_test.ps1`
  `c3ec207d8ed22b946b113827c19579a6e4e9bb7aa8b191e1f2cc78b758574c1d`.
- Shared-tree note: automatic actors published the implementation and initial
  cross-platform tests as `edc9112`, then the summary-level strengthening as
  `36e714c`; the final direct-marker plus human-summary proof is `dc674d3`.
- Resource proof/residual: this adds two metadata predicates after an attempted
  uninstall deletion and no runtime-hook work; no performance claim is made.
  Full deletion suites remain pending permission, and native Windows evidence
  remains pending CI. Pass 21 owns the generated child evaluator deadline.

## Pass 21 record: Bun-owned child deadline and reap

- Fresh agent: `omp_pass_21_child_deadline`.
- Finding: the generated OMP bridge awaited the dcg child's stdout, stderr, and
  exit status without an outer bound. dcg normally enforces its own evaluator
  deadline, but a process-level wedge before or outside that mechanism could
  hang every OMP Bash callback indefinitely.
- Change: the generated direct `Bun.spawn` call now supplies an exact
  30,000-millisecond `timeout` and `SIGKILL` `killSignal`. Bun therefore owns
  both timer and direct-child reap semantics; no competing JavaScript timer,
  abort listener, shell, or detached child was introduced. The ceiling stays
  above dcg's shipped 1-second default and 3-second broad-Windows-preset
  default, while deliberately capping an operator-configured OMP evaluation
  budget longer than 30 seconds. README and agent documentation state that
  policy instead of presenting the outer deadline as invisible behavior.
- Monotonic safety proof: the bridge still concurrently collects stdout,
  stderr, and `proc.exited` before classification. Complete deny, ask, and
  indeterminate JSON remains authoritative even when the eventual forced exit
  is 137; a timeout without a parsed safety verdict emits an infrastructure
  diagnostic and follows the integration's established visible fail-open
  policy. Generated-source assertions pin the native spawn options and forbid
  `Promise.race`, `setTimeout`, and listener substitutes. The replay mock adds
  compressed timeout-without-verdict and timeout-after-deny cells, while
  explicitly not claiming that a compressed mock proves Bun's native timer.
- Native runtime evidence: Bun 1.3.8 directly spawned `/bin/sleep 5` with the
  production option shape, then the production `Promise.all` collection
  completed in about 121 milliseconds under a 100-millisecond probe timeout,
  with exit 137 and both streams drained. This is direct-child kill/reap
  evidence; it created and deleted no files. Bun's official spawn reference and
  child-process documentation independently specify the `timeout` and
  `killSignal` contract.
- Fresh-eyes correction: strict Clippy rejected two runtime assertions over
  constants. They are now one compile-time const assertion block rather than a
  lint suppression or weakened proof. Exact final bytes passed remote
  `cargo check --all-targets`; two independent remote
  `cargo clippy --all-targets -- -D warnings` runs; exact unit tests
  `omp_extension_source_bounds_the_child_evaluator` and
  `omp_extension_source_encodes_monotonic_child_transition_table`; and local
  `cargo fmt --check` plus `git diff --check`. The deletion-bearing generated
  replay was not executed without explicit deletion permission.
- Exact final hashes: `src/cli.rs`
  `004664488a7639185eba21a28c9f470bbe3945f81231dff2c3b69f67a1e7f60f`;
  `README.md`
  `c2c364d005299cccc2c371d69846a632e50ca3cee34a8256c2154bc0d9e31bd3`;
  `docs/agents.md`
  `59206614dcae57fe0059b0a1fd2538dc15024c6d5cca4520006c39882e6f9206`.
  The implementation/docs landed as `bcbb59d`, `84413d3`, and `173faa8`;
  the automatic shared-tree actor preserved the final const-proof cleanup as
  `afdce76`.
- Resource proof/residual: ordinary evaluations add only Bun's constant-size
  native timer bookkeeping. No steady-state latency or throughput gain was
  measured or claimed; the gain is a finite 30-second pathological tail where
  the prior upper bound was infinite. The production command is a direct dcg
  spawn, so the native proof covers the supported process shape. A hypothetical
  future dcg that launches descendants inheriting its pipes could extend stream
  drainage beyond direct-child reap and remains an explicit trust boundary for
  later fault-injection and TCB passes.

## Pass 22 record: persistent robot history with canonical agent attribution

- Fresh agent: `omp_pass_22_history_attribution`.
- Finding: OMP evaluates through `dcg --robot test --stdin --agent omp`, but
  persistent command history existed only in hook mode. Robot evaluation could
  therefore block correctly while leaving no durable audit row, and no row
  could prove the canonical OMP agent identity or effective child cwd.
- Change: robot-mode `test_command` now constructs its history entry only after
  graduation, `--force`, and policy-mode resolution. It records the detected
  canonical agent key, child process cwd, evaluation duration, allowlist
  provenance, and final outcome. Deny/Ask map to deny, Warn to warn, Log and
  final Allow to allow, and deadline Indeterminate to conservative deny with
  `dcg.internal:evaluation-deadline`. Human `dcg test` remains a diagnostic and
  stays outside history. `DCG_HISTORY_DB` retains precedence over the configured
  path. The writer lives inside `test_command`, so it drops and drains before
  the command dispatcher can call `process::exit` for a blocked robot result.
- Mutation-sensitive proof: pure unit controls cover robot/history enablement,
  all policy modes, final forced allow, conservative indeterminate metadata,
  allowlist provenance, canonical `omp`, and a `codex-cli` inverse. The existing
  robot integration suite now contains a real-binary regression that crosses
  blocked exit 1, checks the environment path override against a decoy path,
  requires OMP and Codex rows with separate agent keys, and requires an ordinary
  human OMP diagnostic to add no row. The deletion-bearing tempfile test source
  compiled but was not executed without explicit deletion permission.
- Runtime evidence: the pinned-worker binary SHA-256 was
  `486517c08ffbd2725e6c65240a3c8ae9e343b815e0e781175c8850780d23d4c7`.
  Immediately after blocked OMP exit 1, SQLite contained
  `omp|/data/projects/destructive_command_guard|git reset --hard HEAD|deny|core.git|reset-hard`.
  After a safe Codex robot evaluation and a human OMP diagnostic, exactly two
  rows existed: the OMP deny and a `codex-cli` allow; the human diagnostic added
  none. The non-deleted evidence database remains on `vmi1227854` at
  `/tmp/dcg-pass22-history-887950e/history.sqlite3`, SHA-256
  `ec64b8967ae4eef0c818b79a8eef2ed94c93a85e9f0f4e4673875fe5128a21d9`.
- Verification: one exact pinned-RCH unit run reported 4 passed, 0 failed, 3722
  filtered. The final tree passed pinned-RCH `cargo check --all-targets` and
  `cargo clippy --all-targets -- -D warnings`, plus local
  `cargo fmt --check` and `git diff --check`. The first all-target check caught
  a test closure-lifetime error; the test helper was corrected and the exact
  check rerun green rather than claiming the failed compile.
- Exact final hashes: `src/cli.rs`
  `64e0c0f622558e1753ab760b4226d4c72b0753227add59a8925ddbc32f7c27c8`;
  `tests/robot_mode.rs`
  `8aa2a26aa4a5e7d78f1f6f8dd65febdce943ac5ad53313dbac5d8c0e86470b02`;
  `README.md`
  `1a154567c661809af785cbf35ac2a8d5345b681abad27dd71b753295cb68d1e4`;
  `docs/agents.md`
  `a08a03fc9be5ce108823823d9a37d92e85de2466b8bb2361f8f7d4afab50cde6`.
  Automatic shared-tree commits `bca8b99` and `5aa6fd8` contain implementation,
  unit proofs, and docs; `887950e` contains the real-binary regression source.
- Resource proof/residual: history-disabled robot calls pay only one Boolean
  branch. History-enabled calls intentionally add one writer/thread and one
  SQLite audit row per process, bounded by the evaluation deadline remaining at
  writer construction. History is best-effort: if SQLite cannot acknowledge in
  that remainder, dcg preserves the bounded safety response and the telemetry
  row may be lost. No steady-state performance improvement was measured or
  claimed in this correctness pass.

## Pass 23 record: executable-authoritative Unix OMP detection

- Fresh agent: `omp_pass_23_executable_detection`.
- Finding: the Unix installer treated stale config roots, agent-directory
  overrides, and profile environment state as proof that OMP was installed.
  Its `command -v omp` fallback could also accept a shell function. These
  false positives caused automatic extension configuration on machines with no
  runnable OMP CLI, and version probing could invoke a function shadow instead
  of the external executable that detection intended to identify.
- Change: Unix detection now resolves the external candidate with
  `builtin type -P omp`, requires that candidate to be a regular executable
  (`-f` and `-x`), and passes the exact resolved path to `try_version`. Stale
  state and environment selectors no longer suffice. Explicit Rust
  `dcg install --omp` remains available, and native Windows detection remains
  byte-identical with its existing state-based semantics.
- Mutation-sensitive proof: the four prior state-positive Bats cases are now
  negative controls. Additional controls reject a non-executable PATH entry and
  a function-only `omp`, while a positive case puts a real executable behind
  both `omp()` and `type()` shadows and requires its exact
  `omp/external-1.2.3` version output. The old predicate detects the stale-state
  and function-shadow witnesses. Fresh-eyes runtime probing additionally found
  that Bash 5.3.9 returns status zero from both `type -P` and `command -v` for a
  non-executable PATH file; that discovery caused the final explicit `-f/-x`
  postcondition rather than relying on lookup success alone.
- Verification: `bash -n install.sh`, full `shellcheck install.sh`, Bats test
  discovery (48 tests), and `git diff --check` passed. A safe Bash runtime
  matrix proved stale state, non-executable files, and function-only shadows are
  ignored, while the real `/Users/jemanuel/.bun/bin/omp` behind function
  shadows is detected and reports OMP 18.0.4. The deletion-bearing Bats suite
  was not run without explicit permission. No Rust changed, so Cargo gates were
  not rerun. UBS recognized no supported language and is not credited.
- Exact final hashes: `install.sh`
  `895fdd0f1cfd370f5a3e344c078da90d279742732b25e7e4eb20bbb9efa0b167`;
  `tests/install/install_test.bats`
  `eb5c344abef9197630456f9f4673a38fde6afc8193d801908c2332698aaa3edd`.
  `install.ps1` remained
  `3291ffd04923b702a516686b0d3782f1d56cb76ea5e743372b2e69498799ff1e`
  and `src/cli.rs` remained
  `64e0c0f622558e1753ab760b4226d4c72b0753227add59a8925ddbc32f7c27c8`.
  Implementation and tests landed as `719cec9`.
- Resource proof/residual: Unix detection removes config-root resolution and
  directory probes and adds constant-size metadata checks for one PATH result.
  This is cold installer work, not the per-command hook path; no performance
  gain was measured or claimed. The safe evidence fixture remains at
  `/tmp/dcg-pass23-executable-detection-2ca7407` because deletion was not
  authorized. Pass 24 owns the generated OMP bridge's executable authority.

## Pass 24 record: embedded OMP executable authority

- Fresh agent: `omp_pass_24_embedded_binary_authority`.
- Finding: the generated marker-owned OMP extension embedded the absolute dcg
  executable selected and validated at installation, but
  `process.env.DCG_BIN || DCG_BIN` allowed ambient process state to replace it
  at every invocation. A stale, missing, or hostile override could bypass the
  installation and doctor byte contracts and reach the bridge's documented
  visible infrastructure fail-open path.
- Change: the generated OMP module now passes only its embedded `DCG_BIN` as the
  direct child executable. The generated artifact explains this authority
  boundary, and README/agent docs state that `DCG_BIN` cannot redirect it and
  that moving dcg requires `dcg install --omp --force`. OpenCode's separate
  generator remains byte-for-byte outside this change.
- Mutation-sensitive proof: before the production edit, the exact OMP source
  test failed at `the installed absolute executable must be the direct child
  capability` (0 passed, 1 failed, 3725 filtered; exit 101). Final bytes passed
  the same focused test twice (1 passed, 0 failed, 3725 filtered). The source
  contract both requires a direct `DCG_BIN` spawn and forbids
  `process.env.DCG_BIN`. The generated Bun replay now injects a nonexistent
  ambient decoy while its certificate still requires the embedded executable,
  so a spelling-independent reintroduction of ambient selection turns it red.
- Fresh-eyes corrections: the first static assertion was caught in the adjacent
  OpenCode test and moved before its red result could be credited. A generated
  authority comment was likewise caught in the adjacent OpenCode generator,
  where it would have been false, and moved to the OMP artifact. Documentation
  wrapping was corrected before final gates.
- Verification: exact RCH `cargo check --all-targets` passed on `vmi1264463`
  (build `29991327735218557`, exit 0); exact
  `cargo clippy --all-targets -- -D warnings` passed there with no warnings
  (build `29991327735218564`, exit 0). Exact `cargo fmt --check` and
  `git diff --check` passed. One earlier all-target attempt was cancelled as a
  stale worker run and receives no credit. The ignored Bun executable replay
  was not run because its `tempfile` teardown deletes artifacts; its source is
  strengthened but remains unexecuted this pass. UBS retained no terminal
  result and is not credited.
- Exact final hashes: `src/cli.rs`
  `cc6d28531b04437f71387db4a2e50b8b0e8ed0acd0651746d86f5748a647c9d9`;
  `README.md`
  `778c890c1fe44e78859e4515a297e7cc6b7bcbaa256ec31f9cfd73a7b5bf2517`;
  `docs/agents.md`
  `a792fe3ce4c7a04770e167b2825c8bb8cb17605f6c6fb6be811f1fd34317d6ea`.
  Implementation, proof, and docs landed as `1a0f8cd`.
- Resource proof/residual: each OMP evaluation removes one environment lookup
  and fallback branch; no benchmark was warranted and no material performance
  gain is claimed. Process count, memory shape, I/O, timeout, dialect, and
  verdict policy remain unchanged. Pass 25 owns fault isolation across child
  stdout, stderr, and exit collection.

## Pass 25 record: fault-isolated child observation

- Fresh agent: `omp_pass_25_stream_fault_isolation`.
- Finding: the generated OMP bridge collected stdout, stderr, and child exit
  through fail-fast `Promise.all`. A direct no-file Bun probe proved that a
  completed deny JSON on stdout plus a rejected stderr promise entered the
  catch/fail-open branch, erasing the already-available safety verdict.
- Change: the bridge now registers all three observations concurrently through
  synchronous-throw-safe capability wrappers and awaits `Promise.allSettled`.
  It classifies fulfilled stdout and exit independently, aggregates collection
  failures into one infrastructure diagnostic, and returns early only when no
  blocking signal survived. Deny/ask/indeterminate stdout remains blocking
  through stderr or exit failure; exit 1 remains blocking through stdout
  failure. The child is typed as Bun's precise `ReadableSubprocess`, keeping
  both pipes statically readable across supported Bun 1.3.x declarations.
- Mutation-sensitive proof: the tests-only pre-fix source contract failed
  exactly at its settled-collector assertion (0 passed, 1 failed, 3726
  filtered). Final bytes passed it (1 passed, 0 failed, 3726 filtered). A
  direct no-file Bun matrix returned block/block/block/visible-fail-open/
  visible-fail-open for stderr-fault+deny, stdout-fault+exit1,
  exit-fault+deny, stdout-fault+exit0, and exit-fault+no-verdict. The generated
  replay encodes all five cells and a fail-fast collector mutant; that ignored
  tempfile-deleting replay was strengthened but not executed.
- Fresh-eyes corrections: synchronous Response/property setup was initially
  outside the spawn catch; each observation now converts its own synchronous
  setup fault to a rejection without preventing the other drains. An initial
  `ReturnType<typeof Bun.spawn>` annotation broadened Bun's generic pipe types;
  it was replaced with `Bun.ReadableSubprocess` and pinned by the source test.
  Strict Clippy then found a needless raw-string delimiter in the replay mutant;
  it was corrected and final-byte gates rerun. A redundant cold broader test
  slice was cancelled on a contended worker and receives no credit.
- Verification: final focused test passed on `vmi1227854` job
  `j-29991327735218621`; exact `cargo check --all-targets` passed there as job
  `j-29991327735218623`; exact
  `cargo clippy --all-targets -- -D warnings` passed there as job
  `j-29991327735218625`. Exact `cargo fmt --check` and `git diff --check`
  passed. No deletion-bearing replay ran.
- Exact final hash: `src/cli.rs`
  `aeb7ac925684105de43aec3eee6301814c64d8d12bedf104ecd95f0556f3fb16`.
  Implementation and proof landed as `99d8e48`.
- Resource proof/residual: normal calls retain one child, the same three
  concurrent observations, and the same stream buffering; they add three small
  helper invocations, settled-result objects, and one empty bounded failure
  array. Faults add bounded diagnostic strings. No extra I/O, process, retry,
  timeout, or measured performance gain is claimed. Pass 26 owns bounded robot
  output collection and denial preservation under oversized child output.

## Pass 26 record: bounded compact output and streaming resource authority

- Fresh agent: `omp_pass_26_output_bounds`.
- Finding: the generated bridge used `Response.text()` for both child pipes,
  which buffered to EOF without an explicit ceiling. Its general robot reply
  also cloned and serialized the entire command, making safe stdout scale with
  configured-large input that OMP already retained.
- Change: the bridge requests a private compact envelope containing only the
  decision plus UTF-8-safe bounded reason and rule id. Its exact stdout ceiling
  is `786486` bytes: six-times worst-case JSON escaping of two `65536`-byte
  fields plus the `54`-byte maximal envelope. Stdout and stderr use independent
  observed-byte-growing collectors that continue draining after their caps;
  stdout overflow blocks before parsing any retained prefix, while stderr is
  bounded and remains diagnostic-only. A complete deny-like prefix survives a
  later read fault, but an apparent allow plus unread bytes follows the existing
  visible infrastructure-fail-open policy. Small stdin retains Bun's direct
  byte-array path; configured-large input streams in bounded chunks without
  splitting an astral scalar.
- Mutation-sensitive proof: the source contract accepts production and rejects
  both a restored `.text()` collector and a restored command-echoing envelope.
  The first compiled focused run found a real Clap ordering bug and three stale
  assertions (12 passed, 4 failed, 1 ignored); runtime validation replaced the
  invalid declarative dependency and the exact final hash then passed 16 tests,
  0 failures, with the deletion-bearing replay intentionally ignored.
- Native no-file evidence: Bun 1.3.8 round-tripped and reaped a real `/bin/cat`
  child when an astral scalar crossed the nominal streaming boundary. A dual
  pipe probe retained exactly `786486` stdout bytes and `65536` stderr bytes,
  drained both overflows, preserved split UTF-8 and a deny before a midstream
  read fault, and reaped exit 0. These probes created and deleted no files.
- Fresh-eyes corrections: rule ids received their own bound; cap arithmetic was
  widened to cover both variable fields; eager cap-sized allocations became
  geometric observed-byte growth; the common stdin path reverted to direct
  encoding; surrogate boundaries were made explicit; hostile thrown values are
  bounded and total across cwd, PTY, spawn, and stream failures; and the private
  envelope gained exact allow/deny omission tests. OpenCode remained unchanged.
- Verification on final `src/cli.rs` SHA-256
  `84d05c13caef0d93db0c004c67664dc7fcfc6470e5753bde495c4a58166ec1f1`:
  focused RCH build `29991327735218708` reported 16 passed, 0 failed, 1 ignored;
  `cargo check --all-targets` passed as RCH build `29991327735218721`;
  `cargo clippy --all-targets -- -D warnings` passed as build
  `29991327735218728`; final `cargo fmt --check` and `git diff --check` passed.
  The implementation and proofs landed as `0412e88`.
- Resource proof/residual: ordinary compact allows retain roughly 21 output
  bytes instead of the command-bearing general envelope and do not reserve the
  cap. Retained pipe buffers cannot exceed their explicit ceilings, though
  geometric growth briefly coexists with the prior buffer. Large input avoids
  one command-sized byte array. No end-to-end latency percentage is claimed in
  this correctness pass; pass 27 owns reproducible cold/warm measurement. The
  generated tempfile replay remains unexecuted under the no-deletion rule, and
  no installed real OMP session was exercised.

## Pass 27 record: cold/warm bridge-tail measurement and cache rejection

- Fresh agent: `omp_pass_27_bun_tail_measurement`.
- Mission/result: measured the exact pass-26 bridge against a faithful legacy
  general-envelope/`.text()` counterfactual on one fingerprinted host. No
  candidate cleared the campaign's `>=3%` practical end-to-end threshold with
  coherent tails and unchanged safety semantics, so production remained
  byte-identical.
- Fingerprint: source `d8f501701cda710a85cffa4ed55b083409ee3510`,
  `src/cli.rs` SHA-256
  `84d05c13caef0d93db0c004c67664dc7fcfc6470e5753bde495c4a58166ec1f1`,
  release binary SHA-256
  `e33629a757076d57ff6e5ad992aba16d638a93e21de7b5082d51d14e68e005df`.
  The host was `hetzner2`, Linux `7.0.0-15-generic`, 16-core AMD EPYC Genoa,
  affinity cores 14/15, Bun 1.4.0, and pinned Rust nightly
  `8954863c8`; ambient `DCG_*` was scrubbed under `env -i` with isolated
  HOME/XDG state. Warm load remained admissible at 2.74 to 3.34.
- Warm evidence: persistent Bun, deterministic interleaved AB/BA order, 2,000
  samples per variant/scenario, 24,000 measured dcg children plus warmups, zero
  semantic mismatches, and 24,000 unique child PIDs. Small safe current/legacy
  p50 was 7.239/7.239 ms and small deny 6.935/6.949 ms. At 65,536 bytes, safe
  p50 improved 2.62% and deny 2.38%; safe p99 improved 5.99% but its bootstrap
  interval crossed zero, while deny p99 regressed 0.81%. Ordinary-command
  bootstrap intervals also crossed zero.
- Resource finding: compact output was 21 versus 230 bytes for a small allow,
  133 versus 1,193 bytes for a small denial, 21 versus 65,756 bytes for a
  maximum-size allow, and 133 versus 66,669 bytes for a maximum-size denial.
  This validates the bounded-memory change but is not a general latency win.
- Decomposition: over 2,000 samples each, `/bin/true` child spawn/collection was
  0.773/1.105/1.678 ms p50/p95/p99, bare Bun startup
  7.013/8.900/9.875 ms, compact robot safe 7.206/9.663/10.956 ms, and robot
  deny 6.412/8.815/12.096 ms. Hook full minus `DCG_BYPASS` isolated dcg's
  evaluator at 4.015/5.706/7.244 ms. Robot mode itself ignores the bypass, so
  no invalid robot subtraction was credited.
- Cold and lifecycle proof: an exact production-collector fresh-Bun run with
  2,000 samples per variant showed current small safe/deny p50 regressions of
  0.79%/0.39%, confirming no cold win. Installed OMP source shows extensions
  import and bind once while handlers repeat; all measured callbacks still
  spawned a unique dcg PID. OMP therefore amortizes Bun startup but does not
  retain a Rust evaluator or regex cache across tool calls.
- No-change checks: ordinary p50 gains were only 0.00 to 0.20%; the stable
  maximum-safe p50 gain was only 2.62%; bounded-collector differences were
  microseconds and below 0.1% end to end; OMP already amortizes Bun startup;
  and a persistent dcg daemon would stale executable, config, packs,
  allowlists, cwd policy, and history while changing the visible infrastructure
  fault boundary. No positive-EV cache preserved the full transition model.
- Fresh-eyes correction: the first cold collector was functionally equivalent
  but not allocation-identical. Its artifact was preserved and superseded by
  the exact production geometric collector before cold results were credited.
- Evidence retained without deletion at
  `/tmp/dcg-pass27-d8f5017-20260825T0942Z/evidence/`: `warm-raw.json`
  (`91ad0c2391047db67907d9c0416068829273ceeac30fc2fb9c3abf32be38ab80`),
  `decomposition-raw.json`
  (`40c91210b56f90496b1ee582a67f2a0c2b1cecf6d455faaeb2505b64e2b8b8c8`),
  `cold-exact-raw.json`
  (`3ceb762adfb8e39f3a2dae5f2680d4cbbaeea9ccf5de7fae02983324feb2150d`),
  `collector-raw.json`
  (`e936fd221a0ee92451730aaef1c345f77dc21ec06b638c67a7e8d024b9c3a2ef`),
  and `bootstrap-summary.json`
  (`d86ebf255872c19cb51547b0b5a2c66accd5432c35ad265c88d005527528d582`).
  The original cold artifact remains visibly superseded rather than deleted.
- Gates: local and exact-host `cargo fmt --check` and `git diff --check` passed;
  HEAD, both remote refs, and the worktree remained clean. Pass 27 changed no
  production files, so compiler/lint gates were not repeated. This performance
  result removes one release concern but does not certify release readiness;
  remaining passes and release-blocking suites still control that decision.

## Pass 28 record: bounded ambient authority without policy drift

- Fresh agent: `omp_pass_28_ambient_authority`; the primary agent completed
  final-byte verification after that agent exhausted its turn quota.
- Finding: the generated OMP bridge correctly inherits the operator's process
  environment because dcg allowlist conditions can inspect arbitrary variable
  names. Broad filtering, an allowlist of `DCG_*`, or replacing the child
  environment would therefore change supported policy semantics. However,
  `DCG_FORMAT` is also a Clap environment default: an invalid ambient value was
  rejected before robot mode could force JSON, yielding exit 2 and a visible
  infrastructure fail-open even though the bridge's private protocol has only
  one valid representation.
- Change: the direct child argv now pins `--format json` immediately before the
  private `--omp-bridge-output` selector. Explicit argv wins over the ambient
  default while leaving the original environment byte-for-byte visible to
  environment-conditioned policy. The generated-source contract, executable
  replay argv, README, and agent documentation all bind that authority choice.
- Rejected designs: copying or filtering `process.env` would risk removing
  arbitrary operator policy inputs and adds per-call work; overriding
  `DCG_FORMAT` in the child environment would make a condition on that variable
  observe a fabricated value. Both were rejected before production changes.
  `DCG_BYPASS` remains ignored at the robot boundary, and `DCG_QUIET` cannot
  turn a deny into an allow because the deny exit remains blocking.
- Mutation-sensitive proof: with only the new source invariant present, the
  pre-fix generated bridge failed the focused test at the missing explicit
  format assertion (exit 101). On final bytes, the same focused RCH test passed
  1 test, 0 failed, with 3,731 filtered. A real Linux binary built from the
  final source then ran a causal pair under
  `DCG_FORMAT=definitely-invalid`: without explicit `--format`, Clap rejected
  the value and exited 2; with `--format json`, dcg emitted exactly
  `{"decision":"allow"}` and exited 0.
- Verification: exact `cargo check --all-targets` passed remotely on `hz2`;
  exact `cargo clippy --all-targets -- -D warnings` passed remotely on
  `vmi1227854`; `cargo fmt --check` and `git diff --check` passed locally.
  Three earlier clippy submissions were refused by worker admission and fell
  back locally; each was interrupted before compilation and receives no gate
  credit. The implementation, proof, and documentation landed as `6cd000b`.
- Exact final hashes: `src/cli.rs`
  `bb1fdc023e13c88fd10d4d71f9d22389d7db7c16bc6b85650f53457dca579ff9`;
  `README.md`
  `dc5d879dbc79db18d8f788e12449e1e47da50d4cd5c725b3cd324d147f197eb2`;
  `docs/agents.md`
  `378c2bf0d878b170d2f40d5c5f1a9b12f2de4d1c6fc02e8dbdf1dc1bcc5fb155`.
- Isomorphism/resource proof: valid ambient formats already became JSON in
  robot mode, so the explicit value changes only the invalid-input failure
  path. Child count, environment contents, cwd, dialect, stdin, pipe bounds,
  timeout, verdict lattice, history, and diagnostics are unchanged; the hot
  path adds two constant argv strings and no additional I/O. Other malformed
  Clap-backed environment values, notably `DCG_VERBOSE`, can still cause a
  visible pre-evaluation infrastructure failure. Eliminating that residual
  without corrupting arbitrary environment-conditioned policy requires a
  separate authority design and is not disguised as complete here.

## Pass 29 record: startup/syscall convergence without a shippable lever

- Fresh agent: `omp_pass_29_startup_syscalls`.
- Mission/result: profiled the exact generated bridge, OMP 18.0.4 extension
  loader, Rust child startup, file I/O, and syscall path. Four serious
  hypotheses were measured. None produced a coherent `>=3%` full-callback gain
  with acceptable tails, so every experimental production change was manually
  reverted and the final source remained byte-identical to Pass 28.
- OMP lifecycle/import finding: OMP loads extensions once and resolves their
  virtual host modules through an already-populated module graph. Cached
  imports of settings, `procmgr`, path utilities, and tokenization cost only
  3 to 8 microseconds; standalone cold-Bun import results would therefore be a
  false proxy for repeated callbacks. Lazy/deep import changes were rejected.
- Helper finding: on the repeated TypeScript path, ordinary-command cwd helper
  p50 was 2.8 nanoseconds, structured-cwd p50 was 451 nanoseconds, and a
  leading-`cd` parse p50 was 986 nanoseconds. The helpers preserve OMP path and
  shell-tokenization semantics and are far below the practical gate.
- Config-clone candidate: copy-on-write removed both unconditional full
  `Config` clones when no invocation-local pack/heredoc override was present.
  Six semantic cases matched exactly: default allow/deny, `--with-packs`, a
  heredoc override, valid custom config, and missing-custom-config fallback.
  Across 3,000 samples per variant/scenario in interleaved AB/BA order (12,000
  measured children, 12,000 unique PIDs, zero mismatches), safe p50 changed by
  only +0.005% and deny p50 regressed 0.56%; both p99s regressed about 3.9%.
  The candidate failed decisively and was reverted.
- Session-identity candidate: syscall-to-source tracing showed that five
  layered allowlist lookup methods resolved the Linux process/TTY identity even
  with no `session=true` entry. Per-lookup lazy resolution reduced a safe child
  from 171 to 141 syscalls and removed all three allowlist `/proc` probe pairs;
  a denial fell from 191 to 151 syscalls and from five pairs to one, with the
  remaining pair owned by the independent session tracker. Matching and
  mismatching `DCG_SESSION_ID` controls preserved allow/deny exit codes plus
  exact stdout/stderr bytes.
- Session A/B decision: a second interleaved AB/BA run used 3,000 samples per
  variant/scenario, 12,000 unique measured PIDs, and zero semantic mismatches.
  Safe gains were 3.078% p50, 3.205% p95, 1.088% p99, and 2.551% mean; deny
  gains were 3.051% p50, 0.437% p95, 0.500% p99, and 2.324% mean. The paired
  p50 intervals were positive, but both tail intervals crossed regression and
  the means remained below 3%. Because direct-child timing is an upper bound
  on the full callback gain, the candidate could not clear the declared gate
  and was reverted despite its genuine syscall reduction.
- Fingerprint/provenance: authoritative local HEAD was
  `7c357b6290ffa8f146623e4724164c9e37866d57`; `src/cli.rs` SHA-256 was
  `bb1fdc023e13c88fd10d4d71f9d22389d7db7c16bc6b85650f53457dca579ff9`;
  restored `src/allowlist.rs` SHA-256 was
  `e92fc7db704fe3ef16db8696f1c3fc98c3c2f37ce647f44d8478d3d85b312b01`.
  The exact baseline release binary SHA-256 was
  `8e6c9012d93696fc2bdf4447981d08638a74a68927fcc572a9e79b029f526483`.
  RCH job `j-29991327735218979` built it remotely on `hz2`. The worker's
  unsynchronized `.git` reported stale `6cd000b` metadata and is explicitly
  non-authoritative; the source hashes above bind the actual transferred bytes.
- Evidence: all 39 retained manifest entries at
  `/tmp/dcg-pass29-7c357b6-20260825T1745Z/evidence/` passed
  `sha256sum -c SHA256SUMS`; the manifest SHA-256 is
  `3e04e61bb6ed0bdd95fe73a525c9a627beb19e1afd0fe5ebb22e4581d4cac120`.
  The primary agent independently reran that manifest check. No artifact was
  deleted.
- Final checks/resource proof: `cargo fmt --check` and `git diff --check`
  passed with a clean worktree. Compiler/clippy gates were not repeated because
  no production or test byte survived. The rejected designs preserve the
  required fresh child, cwd-specific config/allowlist policy, binary authority,
  and per-call failure boundary; no release-performance claim is inferred from
  the discarded candidates.

## Pass 30 record: reproducible absolute-latency certificate

- Fresh agents: `omp_pass_30_final_audit` found release-evidence false-green
  paths; `omp_pass_30_test_race_audit` independently reviewed the repaired
  Bash/PowerShell reporters; and `omp_pass_30_final_45b_audit` recomputed the
  final certificate, sample statistics, manifests, and release-suite
  accounting from the retained artifacts. The primary agent applied the
  `alien-artifact-coding` proof-carrying-artifact discipline and the
  `alien-graveyard` reject-and-preserve discipline. The `rch` skill also
  changed the build method after revealing that an RCH worker checkout omits
  `.git`: only the exact local, provenance-bearing release build receives
  credit.
- Finding/change: the previous performance job was a relative comparison that
  could accept a uniform regression, wrote its JSON before calculating the
  absolute verdict, discarded the gate artifact in CI, and did not prove that
  the measured binary came from the recorded source/toolchain. The schema-v4
  certificate now binds the full embedded Git object id, exact `rustc -vV`
  identity, 182 build inputs and aggregate, harness sources, shipped budget,
  effective configuration, binary bytes, and start/end state. It validates the
  wire decision and output contract for every timed invocation, rejects
  duplicate/missing cases, applies a one-sided 95/95 binomial exceedance rule,
  emits an explicit PASS/FAIL/ERROR object before serialization, and retains
  CI artifacts even on failure. Current-only relative cases are reported as
  unbaselined while the absolute shipped-budget gate remains authoritative.
- Fresh-eyes repairs: hook subprocess tests were isolated from host
  config/history/self-heal state. The Unix and PowerShell E2E reporters now
  distinguish pass, fail, skip, and warning counts; enforce their accounting
  invariant; and make `success`/`complete` false when T374 is intentionally
  skipped. JSON mode keeps stdout byte-pure. PowerShell auto-selection now
  rejects a stale in-tree binary, captures a nonzero `--version` status before
  reading anything else, and compares exact SemVer case-sensitively. The
  protocol matrix also verifies each agent's decision field, exit status, and
  stdout/stderr framing against the real binary, including OMP deny, allow,
  and attribution.
- Certified source/artifact: the accepted source is exact clean detached commit
  `45b737e93f68a654088d58fe2c301fec52632e27`. Its frozen read-only release
  executable is
  `/data/projects/dcg-pass30-final-45b737e-20260825a/evidence/dcg-pass30-45b737e-certified`,
  21,261,440 bytes, SHA-256
  `74acb476ad32701f95085a75e79f15e2176d51da4e2fe50bc9a867f37db0156e`,
  with embedded dcg `0.13.0`, full matching Git SHA, and Rust
  `1.98.0-nightly` commit
  `8954863c81df429ebf96ea38a16c76f209995833` dated `2026-06-05` for
  `x86_64-unknown-linux-gnu`. The authoritative 20-entry manifest is
  `PASS30-CERTIFIED-SHA256SUMS-v2`, SHA-256
  `6eb628c42636475ac9c2a3c15b4d190bde5b4b4b796b6f77424b907d412b13bf`;
  independent strict checksum verification passed every entry. The 182-input
  aggregate independently recomputed to
  `c114724e5c9661d01fd931cad6e966076aa1dd9753a98e8febb1a62b1a812df0`.
- Absolute performance evidence: at admissible one-minute loads 9.19 to 8.137,
  all 9 required cases ran 300 paired `full_eval`/`DCG_BYPASS` samples. Every
  retained delta equals `full_eval - bypass`; recomputed median/p95/p99 values
  match; semantic controls all passed; and 0/2,700 samples exceeded the
  500 ms limit derived from the shipped 1,000 ms deadline and 50% margin. The
  worst evaluator p95 was 35.20988 ms (`multi_construct_245`). This pass
  changes the fidelity of the performance proof, not production hot-path
  speed, so its claimed performance improvement is **0%**.
- Release-suite evidence: against that same frozen binary, the 8-agent harness
  passed 38/38 with empty stderr; default E2E produced exactly 373 pass, one
  intentional T374 skip, zero fail/warn, `success=false`, and
  `complete=false`; full E2E produced 374/374 unique T1-T374 passes, zero
  fail/skip/warn, byte-pure JSON, and empty stderr. The release memory suite
  passed 12/12 and its intentional-leak sanity test proved the detector was
  live. Exact `cargo check --all-targets`,
  `cargo clippy --all-targets -- -D warnings`, `cargo fmt --check`, full
  `cargo test`, and the real Windows-GNU
  `agent_hook_output` cross-check passed on exact 45b bytes. The cross-check
  emitted a host cache-database ENOSPC warning after compilation but returned
  success; native PowerShell source SHA
  `476423dba367caaffad510d09fb879c2d702e54ca870da793266b4b4c79b88fd`
  was verified on Windows, while execution remained load-gated.
- Rejected/superseded evidence: T374 relinked the mutable conventional
  `target-local-bound3/release/dcg` path from the certified `74ac...` bytes to
  `f55e12c4...`; its original `release-binary.sha256` is historical only. The
  frozen independent inode and v2 manifest replace it. The first certified
  perf rerun executed zero cases because its overly hermetic PATH hid `rustc`;
  `...attempt2.json` is authoritative. An RCH-built binary lacked Git metadata;
  a local-bound2 build selected stable Rust; incomplete/cancelled builds and
  the malformed v1 final-state/manifest receive no credit and remain preserved.
- Process-integrity disclosure: at `2026-08-25T19:51:15Z`, an earlier Pass 30
  agent moved the sole `.pyc` out of `scripts/__pycache__` in a disposable clone
  and ran `rmdir` on the then-empty directory without authorization. No bytes
  were deleted, but the directory removal violated the campaign rule and is
  recorded rather than hidden. A malformed scout SSH batch may also have
  appended host keys to the operator's local `~/.ssh/known_hosts`; it made no
  repository change. Later probes used an isolated null known-hosts file.
- Residual/release boundary: the certificate measures dcg's generic Bash-hook
  JSON path with `--agent claude-code`, not a full installed OMP Bun
  `ExtensionAPI` callback; OMP's compact bridge is covered separately by the
  protocol harness. No public release exists for the required fleet-install
  suite. Native Windows execution and all later campaign passes remain open,
  so this is not release readiness. During final evidence capture, a concurrent
  actor advanced `main` and both remote branch refs to `6cda6c6` with
  an allowlist optimization after the certified snapshot. Those bytes are
  explicitly outside Pass 30's certificate and enter Pass 31 as incoming drift;
  they are neither overwritten nor falsely credited here.

## Pass 31 record: byte-exact OMP stdin and boundary certificate

- Fresh agents: `omp_pass_31_byte_boundaries` implemented and exercised the
  boundary work; `omp_pass_31_final_audit` found three proof-sensitivity gaps;
  and `omp_pass_31_reaudit` independently audited the corrected final bytes.
  The primary agent applied the `alien-artifact-coding` requirement that an
  executable certificate inventory its entire corpus and the
  `alien-graveyard` discipline of retaining non-blocking follow-up candidates
  without disguising them as completed performance work.
- Correctness finding/change: the generated OMP extension writes raw command
  bytes to dcg stdin without a framing delimiter, but the shared CLI reader
  always removed one terminal LF and an adjacent CR. OMP therefore changed a
  command ending in LF/CRLF, and a newline-only shell no-op became an empty
  input error. The private `--omp-bridge-output` path now preserves every valid
  UTF-8 byte; human `dcg test --stdin` retains its historical one-line-ending
  normalization. Exact-limit, one-byte-over-limit, invalid UTF-8, NUL, TAB,
  ESC, newline-only, CRLF-only, astral, and astral-at-byte-limit cases are
  covered by pure reader tests and hermetic real-binary probes.
- Generated-artifact proof: direct `TextEncoder` and streamed
  `TextEncoderStream` paths are differentially checked for nine inventoried
  cases spanning controls, valid astral pairs, lone high/low and reversed
  surrogates, and code-unit chunk boundaries. The replay certificate now emits
  `stdinCaseIds`, Rust asserts the exact inventory, and an encode-all mutation
  must fail at the large-stream-path assertion. Comments and evidence state the
  real JavaScript contract: unpaired UTF-16 surrogates canonicalize to U+FFFD;
  valid UTF-8 bytes are otherwise preserved.
- Durable seam proof: the eight-agent real-binary matrix now owns OMP
  LF-only, CRLF-only, and NUL/TAB/ESC/newline-plus-destructive-tail cases. Its
  producer is direct quoted argv rather than `eval`; the NUL is emitted by
  `printf` and never stored in a shell variable. A source contract pins these
  cases, the exact private argv, compact stdout byte count, exit status, and
  empty stderr. The complete matrix was not rerun in this pass because its
  existing harness creates temporary directories; the new byte cases were run
  directly against the exact final binary instead.
- Incoming optimization audit: a concurrent actor restored Pass 29's lazy
  allowlist session-identity candidate. It remains semantically coherent and
  reduces ordinary lookups from session resolution to zero calls, while a
  session-scoped lookup resolves once. Warning-as-error Clippy rejected its
  nested `Option<Option<String>>`; the final code uses explicit
  `SessionIdCache::{Unresolved, Unavailable, Resolved}` state. Mutation-sensitive
  tests cover all five layered lookup entry points and deterministically prove
  `Unresolved -> Unavailable` plus one resolver call across two session
  entries, using a monomorphized `FnOnce` helper with no dynamic dispatch or
  new production allocation.
- Exact final source: clean commit
  `799b7b38e607fbc871fdaf8ba22315fd2f10a879`. The 668-file aggregate, excluding
  only `.git/**` and `target/**`, is
  `5f9f1e2eb2f366f5e38a076474e380ba09a5b9b008e6efbd066b62f03caf9192`;
  its retained manifest is
  `/data/projects/dcg-pass31-8c973d4-b6fdab5-20260826T0022Z/source-files.final.sha256`.
  Final SHA-256 values are `src/cli.rs`
  `84b3d89a1b75c9f01273a80852ca67d774df1a88f6c1d84f4ccdba47b72e36ba`,
  `src/allowlist.rs`
  `8caddf66a44c73341bf9e4f186b09fda876ec020dd32ea9a019f1008e58afbef`,
  `src/perf.rs`
  `63274e2871f77ac16b214c4faca1c52a9430bee723bd5b944a123ddc16932532`,
  and `scripts/e2e_harness_matrix.sh`
  `bb052d5043a4e087c46bcd58fd188bcff868a809e6a56b04351b881ef4a59a01`.
- Verification: with Rust `1.98.0-nightly` commit `8954863c8` and a fresh
  one-minute load below 10 before every executable gate,
  `cargo check --all-targets`, `cargo clippy --all-targets -- -D warnings`,
  and `cargo fmt --all -- --check` passed. Focused release tests passed 1 cache
  transition, 2 layered lookup, 2 reader, 1 matrix-contract, and 9 generated
  OMP source-contract cases. Clippy caught and caused repair of one needless
  raw-string delimiter before the final fence.
- Runtime evidence: the final 371,627,032-byte debug executable has SHA-256
  `98a87a4a51f579d4ec7e9f22e09d38ef7c3c555c8358df1b8afcb3ee729794f5`.
  Ten hermetic probes produced exact compact output and empty stderr: LF/CRLF
  no-ops allowed; the control-byte destructive tail denied as
  `core.git:reset-hard`; 65,536 bytes and the exact astral boundary allowed;
  65,537 bytes and invalid UTF-8 failed conservatively; and human LF-only input
  retained the empty-command error. Bun 1.4.0 independently matched all nine
  direct/stream encoder cases at load 4.36.
- Fresh-eyes verdict and boundaries: the corrected audit found no MUST defect;
  its start/end commit, status, four file hashes, and full delta remained
  identical. The installed OMP `ExtensionAPI`/native `procmgr` callback, the
  ignored tempfile-owning full Bun fixture, public fleet installation, and
  native Windows execution remain unclaimed. Two non-blocking candidates are
  retained for later focused passes: check selector relevance before resolving
  a session identity, and record producer/dcg `PIPESTATUS` cells separately in
  the matrix. Pass 29's timing result still fails the declared coherent
  three-percent acceptance threshold, so Pass 31 claims **0%** performance
  improvement despite proving the syscall/resource invariant.
- Provenance disclosure: external automation committed and pushed the source
  changes in several fast-forward commits while agents were working. Agents
  did not rewrite, reset, or falsely reattribute those commits; every final
  proof above is bound to their exact descendant `799b7b3` bytes.

## Pass 32 record: monotone recovery from framed blocking evidence

- Fresh agents: `omp_pass_32_partial_json` performed the implementation pass
  after rereading the full governing documents and both alien skills;
  `omp_pass_32_final_audit` independently fenced and reviewed the resulting
  production parser, transition model, diagnostics, and proof corpus. The
  implementation agent exhausted its service quota before verification, so
  the primary agent completed every executable gate and retained all failed
  attempts rather than treating partial output as success.
- Correctness finding/change: the bridge previously parsed child stdout only
  as one JSON document. A complete LF-delimited `deny`, `ask`, or
  `indeterminate` verdict followed by a truncated second frame or later stream
  fault therefore became `malformed`; public exit-0/exit-2 policy then allowed
  the command. The cold malformed path now scans only LF-delimited frames,
  cheaply selects the exact Rust producer's decision-first prefixes, and
  recovers only a fully parsed blocking verdict as an absorbing witness. It
  never recovers allow, never parses an unterminated suffix, never accepts
  same-line junk, and never infers a decision from a syntactically incomplete
  prefix. Malformed, unknown, and recovered-corruption states are now visible
  infrastructure diagnostics instead of silent fail-open outcomes.
- Fresh-eyes repairs: the auditor found no production MUST defect but caught a
  proof gap: the same-line-junk negative lacked terminal LF, and no negative
  carried a full deny-like prefix with invalid JSON. The final 14-case framing
  corpus adds delimiter-complete same-line junk and invalid deny/ask/
  indeterminate prefixes across all five child outcome classes. A dedicated
  prefix-only parsing mutant must fail at the invalid-deny cell. A separate
  generated-source contract was corrected to include the already-present
  `protocolDetail` disjunct. The ignored Bun gate accepts an explicit fresh
  `DCG_OMP_REPLAY_ROOT`, allowing exact and mutant artifacts to persist without
  test cleanup.
- Executable artifact proof: Bun 1.4.0 executed the exact generated extension,
  35 outcome/verdict transition cells, 70 framing cells, structural tool-call
  cases, bounded stream/Unicode cases, and callback-level collection faults.
  The final green run rejected five independent mutants: exit-0 deny-to-allow,
  dropped framed-block recovery, prefix-only recovery without JSON parsing,
  fail-fast child collection, and whole-command stdin allocation. Two precursor
  runs remain at `/var/tmp/dcg-pass32-bun-21bab-20260826T0323Z` and
  `/var/tmp/dcg-pass32-bun-02dedd-20260826T0328Z`; they exposed over-specific
  mutation-witness assertions and were not deleted. The authoritative green
  tree is `/var/tmp/dcg-pass32-bun-a895-20260826T0331Z`; its exact generated
  bridge SHA-256 is
  `e58372b1d66c6c22e3d8ef6b6815be2ec8c1c5efa0ea2e8973e073306c423f29`.
- Exact source and verification: the accepted code commit is
  `4116997` on top of the externally created Pass 32 commits `29df7ce`,
  `bce0303`, and `bbbd0c3`. `src/cli.rs` SHA-256 is
  `a895066ca165b9b3657cc74a58b00b50a2bd15e3d1a8a2bbcbd17509d50f8cb8`;
  the 668-file aggregate excluding `.git/**` and `target/**` is
  `6c9e54b3c1fa7b2edde2257ac22bb675626f0c86f23869d338f182a660b0fa2d`.
  On `yto`, with Rust `1.98.0-nightly` commit `8954863c8` and a fresh
  one-minute load below 10 before every gate, `cargo check --all-targets`,
  `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all -- --check`,
  all 9 non-ignored generated OMP source contracts, and the exact ignored Bun
  replay certificate passed. The source snapshot intentionally omitted `.git`,
  so vergen emitted non-fatal missing-Git warnings; the source hashes above are
  the authority. The broader repository test suite was not run because its
  unrelated tempdir tests delete their fixtures, which conflicts with the
  campaign's no-deletion constraint.
- Resource/performance proof and boundary: recovery is linear and bounded by
  the existing approximately 768 KiB stdout cap, runs only after whole-buffer
  JSON parsing has already failed, and adds no process or hot-path allocation
  to ordinary valid output. This is a correctness pass with **0%** claimed
  performance improvement. It does not prove an installed OMP
  `ExtensionAPI` callback, native Windows execution, a public fleet install, or
  release readiness; those remain explicit later-pass obligations.

## Pass 33 record: exact exit-status and signal provenance

- Fresh agents: `omp_pass_33_exit_signal_matrix` independently reread the full
  governing documents and both alien skills before investigating and
  implementing the finite child-termination model;
  `omp_pass_33_final_audit` then fenced the candidate and audited production
  control flow, callback fixtures, diagnostic ownership, resource bounds, and
  proof completeness. The primary agent repaired every confirmed audit finding
  and reran the final certificate on the resulting exact bytes.
- Runtime finding/change: a native Bun 1.4.0 probe demonstrated that ordinary
  `exit 137`, manual `SIGKILL`, and timeout `SIGKILL` all report code 137, while
  only `signalCode` distinguishes the latter two; `killed` is true in all three
  and cannot establish provenance. The bridge now observes exit completion and
  the bounded streams independently, reads `signalCode` only after those
  observations settle, and classifies a distinct signal outcome. Exit 1 keeps
  blocking precedence; deny, ask, indeterminate, and stdout overflow remain
  absorbing; other abnormal numeric exits and valid signals stay visible even
  when a blocking verdict controls the action.
- Fresh-eyes repairs: the independent audit found four defects in the initial
  candidate. Signal plus block could print child stderr twice; arbitrary invalid
  exit values could inject newlines, ANSI controls, or hostile stringification
  into diagnostics; abnormal numeric exit status disappeared under a deny-like
  verdict; and an early rejected exit promise could race a later signal. The
  final code records whether the infrastructure diagnostic consumed stderr,
  reports invalid exits by type only, retains abnormal numeric status under a
  block, and reads signal provenance after all independently settled stream/exit
  observations. Exact callback cases cover signal plus stderr, exit-1
  precedence, exit 9/137 plus deny, control/oversized/throwing exit values, and
  an early exit-observation rejection followed by delayed `SIGKILL` at stdout
  EOF. Clippy and rustfmt then caught and caused repair of two needless
  raw-string hashes and one wrapping mismatch; no near-match result was credited
  to the final source hash.
- Executable artifact proof: Bun 1.4.0 executed the exact generated extension,
  42 outcome/verdict transition cells, 20 termination shapes, 84 malformed and
  partial framing cells, 9 stdin/Unicode boundaries, 48 callback cases, and 14
  structural tool-call cases with `probeComplete=true`. Nine targeted mutants
  must turn red: collapsed signal provenance, unsafe signal names, invalid-exit
  value echo, silent abnormal exit, exit-0 deny erasure, fail-fast collection,
  dropped framed blocking evidence, prefix-only JSON recovery, and unbounded
  stdin encoding. The authoritative persistent evidence tree is
  `/var/tmp/dcg-pass33-auditfix-final3-20260826T0452Z`; its generated bridge and
  runner SHA-256 values are
  `eac12cf81a86ec1334bcc930242e382d3221d85953307a56d717807025a310d2`
  and `e74b60884a6746df1e7b22089310ac863613691f6490756de7d8f0dde3e53702`.
  Superseded and failed evidence trees were preserved rather than deleted.
- Exact source and verification: the accepted source commits are `db1695d` and
  `c40403f`; `src/cli.rs`, `README.md`, and `docs/agents.md` have SHA-256 values
  `a6c5b17536c1730f61564973674888f5320d0141e62e84c28577e4cadd2f6975`,
  `ed90692ea3585c1e2bd4829747eee75abbd6ad91d77e44a1b2b8dc10e3290cd6`,
  and `6c97085f77cda6a240163ca752ba5e4dddd4da6f21c8a7026da1d905a6dfe7d7`.
  The matching 668-file aggregate excluding `.git/**`, `target/**`, and macOS
  transport-only AppleDouble `._*` metadata is
  `bac39ec2e8720e16fd9fe763e5802cb5adb83428843ddd6cd737913c28de8046`.
  On `yto`, with Cargo 1.98.0-nightly, rustc commit `8954863c8`, and fresh
  one-minute load below 10 before each executable gate,
  `cargo check --all-targets`, `cargo clippy --all-targets -- -D warnings`,
  `cargo fmt --check`, all 9 focused source contracts, the exact ignored Bun
  replay, and a direct certificate replay passed. An initial check accidentally
  selected the host August rustc and exposed incompatible cached metadata; the
  final chain explicitly pinned `RUSTC` and `RUSTDOC` and used a separate
  persistent target rather than deleting caches. Snapshot vergen warnings are
  expected because `.git` was intentionally excluded.
- Resource/performance proof and boundary: the change adds one independently
  settled exit observation and one bounded post-settlement property read, with
  no extra process, retry, stream, or unbounded retention. This correctness pass
  claims **0%** performance improvement. The broad tempdir-owning suite was not
  run under the no-deletion rule. Installed real-OMP callback behavior, native
  Windows execution, public fleet installation, the absolute shipped-budget
  certificate, and aggregate release readiness remain later-pass obligations.
- Provenance disclosure: source commits were created and pushed by external
  automation while the primary agent was running exact gates. They were not
  rewritten or reattributed; the final proof above is bound to the exact
  descendant `c40403f` source bytes.

## Pass 34 record: bounded child, pipe, and observation lineage

- Fresh agents and audit sequence: `omp_pass_34_spawn_crash_timeout` performed
  full governing-document and alien-skill intake, runtime fault reproduction,
  implementation, mutation construction, and exact final gates.
  `omp_pass_34_final_audit` found one MUST deadline defect and two SHOULD proof
  gaps in the first candidate. After those repairs,
  `omp_pass_34_postfix_audit` completed its substantive read with no finding but
  correctly invalidated its result when unrelated shared-tree commits changed
  the end fence. The primary agent therefore archived commit `1362793` into the
  read-only snapshot
  `/var/tmp/dcg-pass34-postfix-audit-1362793-20260826c`; the fresh
  `omp_pass_34_snapshot_audit` produced a valid identical-hash start/end verdict
  with no remaining MUST, SHOULD, or COULD finding.
- Runtime finding/change: Bun 1.4.0 probes showed that its native timeout bounds
  the direct child but not EOF on inherited pipes. A direct child exited in
  approximately 1--12 milliseconds while a descendant held stdout or stderr
  for approximately 804--1,005 milliseconds despite a 100-millisecond child
  timeout. The generated bridge now records one monotonic absolute observation
  deadline 30.5 seconds after successful spawn, observes capped stdout, capped
  stderr, and exit concurrently, and rearms its sole timer after exit for
  `min(250 ms, remaining absolute budget)`. Boundary expiry cancels local
  readers while retaining all prior bytes and overflow. Rejected exit
  observation attempts exactly one direct-child `SIGKILL`; there is no retry,
  replacement process, or process-group claim.
- Fresh-eyes repairs: the first implementation cleared the hard deadline and
  granted a fresh 250 milliseconds, so exit at T-minus-1 could move the claimed
  outer limit. The final observer instead clamps to the recorded absolute time,
  retains hard-deadline provenance when the remaining budget wins, and never
  re-kills from the rearmed timer. The replay uses a virtual monotonic clock and
  proves T-minus-1 completion at exactly T with peak one active watchdog, zero
  dead-child re-kills, and no late diagnostics/rejections. The fake no longer
  manufactures `signalCode` merely because `kill()` was requested; null and
  delayed signal cases pin exact diagnostics. Standards-compliant underlying
  cancellation rejection is executed for retained deny and retained overflow,
  with one cancellation owner, released reader lock, absorbing block, one
  boundary diagnostic, and zero unhandled rejection.
- Executable artifact proof: the final Bun certificate has 226 duplicate-free
  cases: 14 tool shapes, 42 transition cells, 20 termination shapes, 84 framing
  cells, 9 stdin/Unicode boundaries, and 57 callback cases. It recorded 105
  watchdog schedules, 9 expiries, peak active watchdog count 1, zero unhandled
  rejections, and `probeComplete=true`. Fourteen causal mutants must turn red:
  exit-0 deny erasure, collapsed signal provenance, unsafe signal name,
  invalid-exit echo, silent abnormal exit, dropped framed block, prefix-only
  parsing, fail-fast collection, plain unbounded `allSettled`, relative
  post-exit deadline, dropped retained block, uncleared watchdog, missing
  rejection-path kill, and unbounded stdin. The authoritative remote evidence
  root is `/var/tmp/dcg-pass34-absolute-final-063d185-20260826b`; generated
  bridge, runner, and direct-certificate SHA-256 values are
  `4baa02a4e260eb7cba1250c29aece0ed08a9d578ef750b2b4dfe0aaea11163e6`,
  `f6d4422daec3301a791e386b585faf0cf669aa1e69878eb1a8fc5be08a4f56e6`,
  and `7af268bd9db96443b953df57e47d96963e0b409b58fbeff0e8bc4b466a95075a`.
  Failed formatter/replay/mutation-witness attempts remain preserved.
- Exact source and verification: accepted source commit `1362793` has
  `src/cli.rs`, `README.md`, and `docs/agents.md` SHA-256 values
  `8ed75fb27a9a259c1541591aae87c35244338eb6394f7f972052697a27d3be56`,
  `27000b86ce2b0f7e7a399a2accfd85201dc6449de2724bdbcdfb5d2ae52a6d55`,
  and `988e5304bd1281b21fa0540470b698b76b6b130d4b7f6b71102e7d26c5809ab1`.
  Its aggregate excluding `.git/**`, `target/**`, and AppleDouble metadata is
  `52dd5b3840d28415066fec0e8c9e70f7380095b38fa6b40f0d4e4974c8c2c042`.
  On `yto`, every executable gate was admitted below load 10 and used Cargo
  1.98.0-nightly, rustc/rustdoc commit `8954863c8`, Bun 1.4.0, explicit
  `RUSTC`/`RUSTDOC`, and the RCH bypass. Final `cargo fmt --check`, 9/9 focused
  source contracts, exact ignored Bun replay plus all mutants, direct
  certificate, `cargo check --all-targets`, and
  `cargo clippy --all-targets -- -D warnings` passed at preceding one-minute
  loads 2.70, 2.28, 1.99, 2.36, 2.12, and 5.13 respectively.
- Resource/performance proof and residual boundary: retained stdout/stderr and
  diagnostics remain capped; stdin remains chunked; the normal bridge adds one
  `AbortController`, bounded wrappers, and at most one active timer. This is a
  correctness/tail-bounding pass with **0%** claimed performance improvement.
  In-process timers cannot preempt synchronous `Bun.spawn`, another synchronous
  runtime fault, or an event-loop stall. `proc.kill()` addresses only the direct
  child, and a non-standard stream whose synchronous cancel throws without
  settling its pending read remains outside the WHATWG/Bun boundary. Installed
  real-OMP behavior, native Windows, public fleet installation, later campaign
  passes, and aggregate release readiness remain unclaimed.
- Provenance disclosure: external automation committed and synchronized the
  exact frozen source as `1362793` while the implementation agent was running
  end-fence checks; byte hashes and checksum-mode comparison proved identity.
  Later unrelated external commits changed the live tree, so final audit credit
  comes only from the immutable `1362793` archive rather than a drifting
  descendant checkout. No peer bytes were reset, stashed, overwritten, or
  included in the Pass 34 ledger change.

## Pass 35 record: pathname authority without byte-identity theater

- Fresh agents and audit sequence: `omp_pass_35_binary_toctou` traced the full
  install-to-spawn replacement lineage after governing-document and alien-skill
  intake. `omp_pass_35_final_audit` found the remaining public README claim to
  be a MUST correction. After that repair, the independent hash-fenced
  `omp_pass_35_postfix_audit` reported no MUST, SHOULD, or COULD finding and no
  byte drift.
- Finding and bounded change: the bridge embeds the installer's absolute dcg
  pathname and refuses ambient `DCG_BIN` redirection, but Bun resolves that
  pathname again at each spawn. It therefore never established the previously
  implied exact executable-byte identity. The generated extension comment,
  detailed agent documentation, and README now distinguish pathname authority
  from hashes, inode/file IDs, signatures, or immutable executable objects;
  state that same-path replacement changes a later callback's executable; and
  limit `dcg doctor` to inspection-time generated-source equality. The explicit
  rebind remains `/desired/path/dcg install --omp --force` (optionally
  `--project`) followed by restarting OMP, with the binary, extension, and
  parent directories documented as trusted write surfaces.
- Rejected mechanism: per-call hashing, metadata caching, inode/file-ID checks,
  or extension-side signature checks were not added. Bun exposes a pathname
  spawn boundary rather than a portable pre-opened executable capability, so
  each proposed check leaves a check-to-execute replacement race while adding
  work to every guarded command. This pass narrows the claim instead of
  shipping an expensive non-proof.
- Mutation-sensitive contract: the generated-source test round-trips the
  embedded absolute path, requires it as Bun's direct spawn operand, rejects a
  bare path lookup and ambient `process.env.DCG_BIN`, positively requires the
  pathname/no-byte-attestation wording, and negatively rejects the former
  `exact dcg binary` claim. The focused test passed 1/1 and
  `cargo check --all-targets` passed against the persistent source snapshot.
- Exact source and audit evidence: accepted source commit `bf4b66b` has
  `src/cli.rs`, `docs/agents.md`, and `README.md` SHA-256 values
  `b65f6c924857516f893ed92910dfc875808c4591a3d9a87199fc9a9a8dce4bae`,
  `5063b79556cc1dda5b85a55ff2e0c880c203f1f3ad757572d25485e320a924d9`,
  and `bbf318c050ca5086575e92cf402cceb7387480990110efe15d932928f130e072`.
  The final auditor's working-tree README hash also included separate unstaged
  peer documentation and was therefore deliberately not used as the commit
  identity. `git diff --check` passed, and only the isolated OMP README hunk was
  staged; all peer hunks remained unstaged. Persistent remote evidence is under
  `/var/tmp/dcg-pass35-path-trust-b9cfb78-20260826a`.
- Verification boundary and infrastructure failures: repository-wide rustfmt
  reported only pre-existing or peer-owned hunks outside the Pass 35 OMP delta,
  so no formatting-green claim is made. The first clippy attempt mixed the
  host's August `cargo-clippy` with the pinned June Cargo; after pinning the
  complete toolchain path, two further attempts failed before linting dcg
  because remote dependency-info files and then the pinned rustc executable
  disappeared. The exact Bun replay likewise failed before test execution when
  Cargo registry and AWS-LC source files disappeared mid-build. No cache was
  deleted and no cold retry loop was continued. These are release-gate holds,
  not source passes.
- Resource/performance and no-claim boundary: production execution is
  byte-for-byte unchanged apart from a generated comment, so the pass adds no
  process, I/O, allocation, or hot-path work and claims **0%** measured
  performance improvement. It does not prove atomic check-and-execute,
  executable-byte identity, same-path replacement resistance, what an already
  running OMP session loaded, native Windows runtime behavior, public fleet
  installation, or aggregate release readiness.

## Pass 36 record: no-follow extension ownership and exact-cwd cleanup

- Fresh agents and audit sequence: `omp_pass_36_path_aliases` constructed the
  cross-platform alias/ownership matrix and found two MUST defects. The first
  post-fix auditor found a third MUST at the doctor detection gate; after that
  repair, `omp_pass_36_final_audit` found one inaccurate remediation. The final
  hash-fenced `omp_pass_36_remediation_audit` reported no remaining MUST,
  SHOULD, or COULD finding and no scoped byte drift. All agents applied the
  requested alien-artifact state/invariant lens and alien-graveyard
  failure-lineage lens.
- Findings and bounded changes: Rust's followed `Path::exists()` treated a
  dangling final extension symlink as absent, skipped ownership inspection,
  and allowed the shared atomic writer to replace the foreign directory entry.
  Doctor's initial OMP-presence gate had the same followed-path blind spot.
  Both now use `symlink_metadata`; only a genuine missing entry is `Absent`,
  while metadata/read failures remain errors and neither ordinary install,
  `--force`, nor doctor repair gains replacement authority. Valid marker-owned
  symlinks retain their intentional follow-and-refresh behavior. Windows
  uninstall now uses PowerShell's exact current location instead of walking to
  a Git ancestor, matching Rust and Unix OMP cwd-only discovery. Doctor's two
  renderers now truthfully distinguish user/profile reinstall from
  current-directory `--project` reinstall after the reported path/profile
  problem is resolved.
- Mutation-sensitive evidence: the opt-in Rust regression creates a dangling
  project extension under a caller-owned persistent root, isolates `HOME`,
  removes OMP/PI variables, requires no `omp` executable on `PATH`, and proves
  the doctor presence gate, direct inspection, ordinary install, forced
  install, and repair all refuse while the link and absent target remain
  unchanged. Replacing no-follow presence with `Path::exists()` makes the
  doctor-gate and entry assertions red. PowerShell tests plant both an active
  nested-cwd extension and byte-recorded Git-ancestor decoy; reverting to the
  ancestor walker fails both removal and preservation assertions. A separate
  bracketed cwd keeps literal-path coverage.
- Exact source and execution evidence: accepted source commit `289f565` has
  SHA-256 values
  `9fe8c67f46ef4f7138eb54798df261de06d758003d32bccf1443beaf80f3686f`
  (`src/cli.rs`),
  `ea3b28cb9a0b078137e5c30fcbe89abd0a0150a038a1bdb27a6513802788bcda`
  (`uninstall.ps1`), and
  `a4fa7eb373765c9b1a442d67a66980d85b5aea18c9527c3ae71be28ea1e9d4ad`
  (`tests/installer/uninstall_test.ps1`). On low-load `hz2`, the final Rust
  bytes passed strict all-target Clippy, `cargo check --all-targets`, and the
  exact ignored regression (1 passed, 0 failed, 3752 filtered). The preserved
  final filesystem witness is
  `/var/tmp/dcg-pass36-dangling-1fba8fa-20260826c`; remote and local source
  hashes matched, `symlink_metadata` still reported a symbolic link, and its
  target remained absent. `git diff --check` passed.
- Formatting and platform proof boundary: exact-source `cargo fmt --check`
  named only the same pre-existing/peer-owned hunks later in `src/cli.rs`,
  `src/evaluator.rs`, and `src/packs/core/filesystem.rs`; no Pass 36 hunk was
  listed, so those unrelated bytes were not changed and no repository-wide
  formatting-green claim is made. The PowerShell suite owns deletion fixtures
  and was not executed under the no-deletion rule. Native Windows
  reparse/junction behavior and parser/runtime execution therefore remain for
  later Windows passes/CI rather than being inferred from static source.
- Resource/performance and residual boundary: the hook evaluator and generated
  bridge hot paths are unchanged, so measured performance improvement remains
  **0%**. The Rust lifecycle adds at most one metadata syscall during OMP
  detection/install/doctor; Windows removes an ancestor walk in favor of one
  location lookup. Same-writer check/use races remain an explicit threat-model
  residual for Pass 39, and FIFO/device and hard-link policy deserve separate
  bounded coverage rather than expanding this pass's deletion authority. No
  public artifact, fleet install, or aggregate release-readiness claim is made.

## Pass 37 record: lossless OMP environment-path resolution

- Fresh agents and audit sequence: `omp_pass_37_windows_paths` differentially
  audited Rust, Unix, and PowerShell path/profile resolution against current
  OMP/Bun and Node path behavior. `omp_pass_37_postfix` then read the first
  candidate with a fresh hash fence, rejected a platform-wide UTF-8 conversion
  as overbroad, and required explicit preservation of native Windows wide
  values. After the implementation was restricted to `cfg(unix)` and the
  Windows lone-surrogate contract test was added, the auditor reported no
  remaining source-level MUST. Both agents used the alien-artifact invariant
  lens and alien-graveyard failure-lineage lens.
- Finding and bounded change: on Unix, Rust's `var_os` preserved arbitrary raw
  bytes from `PI_CODING_AGENT_DIR`, but OMP's Bun/JavaScript environment exposed
  the same bytes as a string containing U+FFFD. dcg could therefore report a
  successful install under a raw-byte pathname that OMP would never load. The
  default-profile resolver now rejects non-UTF-8 Unix overrides before any
  write, with a diagnostic naming `PI_CODING_AGENT_DIR` and the JavaScript
  string boundary. Named profiles still ignore the legacy override before it
  is inspected, project scope remains independent of the user agent directory,
  and Windows retains the original `OsString`/UTF-16 value without lossy
  conversion.
- Mutation-sensitive and cross-language evidence: Bun 1.4.0, launched through
  a byte-preserving `execve`, decoded `relative\xffname` as
  `relative\u{fffd}name`. The pre-fix binary at `4f1bf06` then exited 0,
  announced success, created the raw-byte extension, and did not create the
  replacement-character path; that red witness remains under
  `/var/tmp/dcg-pass37-nonunicode-red-4f1bf06-20260826a`. Node's `path.win32`
  oracle and the existing Rust/PowerShell matrix agreed for drive, UNC,
  rooted, separator, and reserved-device cases; dcg's rejection of a
  drive-qualified Windows config name remains deliberately conservative.
- Exact verification: accepted commit `d41abb5` has `src/cli.rs` SHA-256
  `f45ed97b51bab891fae640b461e830bce75760db102a70727a199b18c7784a88`.
  The frozen RCH overlay at
  `/data/tmp/rch/destructive_command_guard/aa40d3d0fede8882` matched that hash
  and passed `cargo check --all-targets`, strict all-target Clippy, and the exact
  Unix raw-byte unit test (1 passed, 0 failed, 3753 filtered). The final
  real-binary certificate at
  `/var/tmp/dcg-pass37-nonutf8-green-f45ed97-20260826a/matrix.json` has SHA-256
  `a3349ea951996601c6984d573c3da7937dd9fbf1b210d620366ff78bdf1a3b0b`
  and eight passing cases: absent, empty/default OMP, and default PI profiles
  fail closed without raw, replacement, or fallback writes; named OMP and PI
  profiles install canonically; project scope installs at exact cwd; and strict
  JSON doctor returns one `omp_extension` error with the precise variable.
  The tested binary SHA-256 is
  `1cb41b3ccee90f537aedc44f41195632b248b28d505bbd935a09aab7d7e4d23d`.
- Formatting, platform, and no-claim boundary: `cargo fmt --check` listed only
  pre-existing formatting debt later in `src/cli.rs`, `src/evaluator.rs`, and
  `src/packs/core/filesystem.rs`; no Pass 37 hunk was listed, so peer bytes were
  preserved and no repository-wide formatting-green claim is made. After the
  missing Rust Windows standard library was installed on the remote verification
  toolchain, the cross-check advanced but stopped in `aws-lc-sys` because the
  worker lacks `x86_64-w64-mingw32-gcc`. Thus the Windows test has source-audit
  coverage but neither a complete Windows cross-compile nor native PowerShell
  runtime credit. This lifecycle-only validation performs one linear `to_str`
  check on Unix default-profile install/doctor resolution; the evaluator and
  generated bridge hot paths are unchanged, so the pass claims **0%** measured
  performance improvement. Public fleet installation and aggregate release
  readiness remain unclaimed.
